% !TeX root = ../main.tex
\documentclass[../main.tex]{subfiles}

\begin{document}

\section{Implementación}

La sección anterior ha descrito el diseño del sistema desde una perspectiva arquitectónica: bloques funcionales, contratos entre módulos y decisiones de diseño adoptadas para transformar una señal EEG real en una salida MIDI física. En esta sección se detalla cómo se ha materializado esa arquitectura en código, indicando qué responsabilidades asume cada parte del sistema y cómo circulan los datos entre ellas.

El criterio seguido en la implementación es mantener separadas las capas de adquisición, transporte, procesamiento, sonificación y salida MIDI. De esta forma, el firmware del microcontrolador se concentra en las tareas de tiempo real asociadas al ADS1299, mientras que el entorno Linux/Python de la Arduino UNO Q recibe los bloques EEG ya escalados, calcula características temporales y espectrales, evalúa la calidad de la señal y genera los eventos musicales. Esta separación evita que la lógica musical dependa directamente de detalles de bajo nivel del convertidor analógico-digital y permite analizar cada etapa de forma independiente.

\subsection{Implementación del firmware de adquisición}

El firmware de adquisición constituye la primera capa ejecutable del sistema. Su papel es transformar la salida digital del ADS1299 en una señal EEG preparada para el backend Python. Para ello configura el convertidor, espera a que haya una nueva conversión disponible, lee el frame por SPI, reconstruye las muestras, las escala a unidades físicas, aplica un filtrado digital básico y agrupa los datos en bloques mediante el evento \texttt{eeg\_block\_uV}.

Dentro del flujo general del sistema, esta capa se encarga de las tareas más cercanas al hardware y al tiempo real. El microcontrolador asegura que las muestras lleguen a Python con una cadencia coherente, en microvoltios y con una estructura de bloque estable. A partir de esos bloques, el backend Python realiza el almacenamiento temporal, el análisis espectral, la evaluación de calidad y la generación de parámetros para la sonificación.

La Figura~\ref{fig:impl_firmware_flujo_adquisicion} resume esta cadena de transformación, desde el frame nativo del ADS1299 hasta el bloque \texttt{eeg\_block\_uV} consumido por el backend Python.

\begin{figure}[H]
    \centering
    \IfFileExists{images/uml/impl_firmware_flujo_adquisicion.pdf}{%
        \includegraphics[width=0.92\textwidth]{images/uml/impl_firmware_flujo_adquisicion.pdf}
    }{%
        \fbox{\parbox{0.9\textwidth}{\centering Diagrama pendiente de exportar desde Mermaid: \texttt{images/uml/impl\_firmware\_flujo\_adquisicion.mmd}}}%
    }
    \caption{Flujo de adquisición implementado en el firmware. El microcontrolador lee frames RDATAC del ADS1299, valida el campo de estado, reconstruye las muestras de 24 bits, las escala, aplica filtrado digital y publica bloques de 8 muestras hacia Linux.}
    \label{fig:impl_firmware_flujo_adquisicion}
\end{figure}

La implementación se reparte en varios archivos. \texttt{sketch.ino} actúa como programa principal: inicializa Bridge, Monitor, ADS1299, filtros y handlers de comunicación, y ejecuta el bucle de adquisición. \texttt{ADS1299\_SafeSPI} encapsula el transporte SPI de bajo nivel; \texttt{ADS1299Plus} implementa la secuencia de arranque, los comandos, el acceso a registros y la lectura de frames; \texttt{ADS1299\_Registers.h} recoge comandos y direcciones con nombres legibles; y \texttt{filters.h} y \texttt{streaming.h} contienen, respectivamente, el acondicionamiento digital y la publicación de bloques hacia Linux.

\subsubsection{Configuración del ADS1299}

El ADS1299-4 es un convertidor analógico-digital de cuatro canales con 24 bits de resolución, orientado a señales EEG y biopotenciales. Integra amplificador de ganancia programable, referencia de tensión, oscilador y filtro digital de decimación. En este proyecto trabaja a \SI{250}{\hertz}, con ganancia 24 y referencia interna de \SI{4.5}{\volt}.

La configuración del ADS1299 se realiza en dos etapas. Primero, el firmware deja el convertidor en un estado conocido mediante \texttt{ADS1299Plus::begin()}. En esta fase se inicializa la comunicación SPI, se aplica un reinicio del dispositivo, se detiene cualquier conversión que pudiera estar activa y se fuerza el modo \texttt{SDATAC}. Este modo permite escribir y leer registros con seguridad antes de iniciar la adquisición continua. Finalmente, se lee el registro de identificación para comprobar que el circuito detectado corresponde a la familia ADS1299 y a la variante de cuatro canales usada en el prototipo.

Después de esta inicialización, \texttt{configureDefaults()} carga la configuración base del convertidor. Esta función programa los registros que fijan la tasa de datos, la referencia interna, el estado de cada canal, la derivación de BIAS y la detección \textit{lead-off}. Por ejemplo, \texttt{CONFIG1} define la frecuencia de muestreo, \texttt{CONFIG3} habilita la referencia interna y el bloque BIAS, \texttt{CHnSET} configura cada canal y su ganacia, y \texttt{BIAS\_SENSP}/\texttt{BIAS\_SENSN} seleccionan qué entradas participan en la señal de BIAS. De esta forma, el programa principal no contiene valores binarios dispersos, sino una configuración agrupada y más fácil de revisar.


En la versión final del prototipo se utiliza la configruación, resumidoa en la Tabla~\ref{tab:impl_ads1299_modo_final}.
\begin{table}[H]
    \centering
    \caption{Configuración final del ADS1299 en el modo de adquisición empleado.}
    \label{tab:impl_ads1299_modo_final}
    \begin{tabular}{p{0.22\textwidth}p{0.33\textwidth}p{0.34\textwidth}}
        \hline
        \textbf{Elemento} & \textbf{Configuración final} & \textbf{Papel en la adquisición} \\
        \hline
        CH1 & Activo, ganancia 24, entrada normal & Canal EEG principal usado por el procesamiento y la sonificación. \\
        CH2--CH4 & Apagados/cortocircuitados en el modo final & Conservados en el payload por contrato, sin interpretarse como EEG activo principal. \\
        BIAS & Derivado de CH1P + CH1N & Referencia de modo común limitada al canal usado. \\
        \textit{Lead-off} & Desactivado & Evita inyección diagnóstica durante la captura final. \\
        Test interno & Desactivado & No se mezcla señal de prueba con la señal real. \\
        Tasa de muestreo & \SI{250}{\hertz} & Frecuencia común entre firmware y backend Python. \\
        \hline
    \end{tabular}
\end{table}

Esta distinción evita una ambigüedad importante: el sistema mantiene un formato de datos de cuatro canales porque el ADS1299-4, el firmware, las capturas y el backend comparten ese contrato. Sin embargo, la validación experimental final se centra en CH1 como canal fisiológico principal.

\subsubsection{Sincronización temporal mediante DRDY y RDATAC}

Una vez configurado el ADS1299, el firmware inicia la adquisición manteniendo activa la señal \texttt{START} y enviando el comando \texttt{RDATAC}. En este modo, el convertidor genera frames de forma continua. La señal \texttt{DRDY}, activa a nivel bajo, indica que una nueva conversión está lista para ser leída.

El firmware no genera muestras por temporización software. Sigue el ritmo impuesto por el ADS1299. Cuando \texttt{DRDY} baja, una interrupción muy breve incrementa el contador \texttt{drdy\_count}. La lectura SPI no se realiza dentro de la interrupción, porque una ISR debe ser corta y no conviene ejecutar operaciones complicadas. Después, el bucle principal consulta el contador y, si existe una conversión pendiente, llama a \texttt{ADS1299Plus::readFrameRDATAC()}.

Cada frame RDATAC leído en esta configuración ocupa \SI{15}{\byte}: tres bytes de estado y cuatro canales de tres bytes cada uno. El driver baja \texttt{CS}, transfiere quince bytes por SPI enviando \texttt{0x00} como byte de relleno y vuelve a subir \texttt{CS}. El resultado se separa en un campo \texttt{status24} y cuatro muestras de canal.

La Figura~\ref{fig:impl_secuencia_drdy_rdatac} resume esta secuencia. La clave es que \texttt{DRDY} solo marca disponibilidad de dato; la transformación de la muestra se realiza después en el bucle principal.

\begin{figure}[H]
    \centering
    \IfFileExists{images/uml/impl_secuencia_drdy_rdatac.pdf}{%
        \includegraphics[width=0.90\textwidth]{images/uml/impl_secuencia_drdy_rdatac.pdf}
    }{%
        \fbox{\parbox{0.9\textwidth}{\centering Diagrama pendiente de exportar desde Mermaid: \texttt{images/uml/impl\_secuencia\_drdy\_rdatac.mmd}}}%
    }
    \caption{Secuencia simplificada de adquisición. La interrupción de \texttt{DRDY} solo marca la disponibilidad de una muestra; la lectura SPI, el filtrado y la publicación del bloque se realizan posteriormente en el bucle principal.}
    \label{fig:impl_secuencia_drdy_rdatac}
\end{figure}

\subsubsection{Reconstrucción, escalado y filtrado de muestras}

Cada frame RDATAC comienza con un campo de estado de 24 bits. El firmware valida que los bits más significativos coincidan con el prefijo esperado \texttt{0xC00000}. Esta comprobación permite detectar frames incoherentes o problemas de sincronía en la comunicación SPI.

Después del estado aparecen las muestras de canal. Cada canal se transmite como tres bytes en orden \textit{MSB first}. El driver reconstruye esos tres bytes mediante \texttt{unpack24()} y realiza la extensión de signo a 32 bits. Este paso es necesario porque el ADS1299 entrega valores con signo; sin esta extensión, una muestra negativa podría interpretarse como un valor positivo de gran magnitud.

Una vez reconstruido el valor digital de cada canal, el firmware aplica el factor de conversión \texttt{LSB\_V}. En la configuración utilizada, este factor vale \SI{2.235e-8}{\volt} por unidad digital del ADC. Por tanto, la conversión inicial a tensión se expresa como:

\[
    v = c_{\mathrm{ADC}} \cdot \texttt{LSB\_V}
\]

donde \(c_{\mathrm{ADC}}\) es el valor digital con signo reconstruido a partir del frame RDATAC.

La señal resultante se mantiene en voltios para aplicar el acondicionamiento digital: eliminación de corriente continua con filtro paso alto de \SI{0.5}{\hertz}, filtro notch de \SI{50}{\hertz} y paso bajo de \SI{40}{\hertz}. Tras el filtrado, la muestra se convierte a microvoltios enteros mediante \texttt{volts\_to\_uV\_i32()}, de forma que el backend recibe valores en microvoltios y no valores digitales crudos del ADC.

La Figura~\ref{fig:impl_frame_rdatac_a_bloque_uv} muestra la transformación entre el frame nativo del ADS1299 y el bloque que finalmente recibe Python. La figura ayuda a distinguir dos niveles: un frame RDATAC representa una muestra multicanal; un bloque \texttt{eeg\_block\_uV} agrupa ocho frames ya validados, escalados y filtrados.

\begin{figure}[H]
    \centering
    \IfFileExists{images/uml/impl_frame_rdatac_a_bloque_uv.pdf}{%
        \includegraphics[width=0.80\textwidth]{images/uml/impl_frame_rdatac_a_bloque_uv.pdf}
    }{%
        \fbox{\parbox{0.9\textwidth}{\centering Diagrama pendiente de exportar desde Mermaid: \texttt{images/uml/impl\_frame\_rdatac\_a\_bloque\_uv.mmd}}}%
    }
    \caption{Transformación entre un frame RDATAC del ADS1299 y el bloque \texttt{eeg\_block\_uV}. Cada frame individual se valida, se reconstruye, se escala y se filtra; después se acumulan ocho muestras para formar el bloque enviado a Python.}
    \label{fig:impl_frame_rdatac_a_bloque_uv}
\end{figure}

\subsubsection{Agrupación y envío de bloques hacia Linux}

El firmware no publica una notificación por cada muestra individual. En su lugar, acumula ocho muestras filtradas en la estructura \texttt{EegBlockUV}, definida en \texttt{streaming.h}. Con una frecuencia de muestreo de \SI{250}{\hertz}, cada bloque representa \SI{32}{\milli\second} de señal.

Cada bloque contiene una cabecera con \texttt{block\_idx}, \texttt{first\_sample\_idx} y \texttt{sample\_count}. Después incluye, para cada una de las ocho muestras, el \texttt{status} del ADS1299 y los cuatro canales expresados en microvoltios. Esta estructura define el contrato entre el firmware y Python.

\begin{table}[H]
    \centering
    \caption{Relación temporal entre muestra, bloque y transporte en la adquisición.}
    \label{tab:impl_tiempos_adquisicion}
    \begin{tabular}{p{0.28\textwidth}p{0.25\textwidth}p{0.36\textwidth}}
        \hline
        \textbf{Elemento} & \textbf{Valor} & \textbf{Significado} \\
        \hline
        Frecuencia de muestreo & \SI{250}{\hertz} & Una muestra nueva aproximadamente cada \SI{4}{\milli\second}. \\
        Tamaño de bloque & 8 muestras & Agrupación mínima antes de publicar por Bridge. \\
        Periodo nominal de bloque & \SI{32}{\milli\second} & Intervalo temporal representado por cada \texttt{eeg\_block\_uV}. \\
        Canales por muestra & 4 & Contrato estructural común a firmware, backend y capturas. \\
        \hline
    \end{tabular}
\end{table}

Cuando un bloque se completa, se introduce en una cola circular \texttt{TxBlockRing}. Esta cola desacopla la adquisición de la publicación por Bridge y permite que el firmware siga adquiriendo aunque el envío de un bloque tarde algo más que una iteración del bucle. La publicación se realiza mediante \texttt{Bridge.notify("eeg\_block\_uV", ...)}, enviando la cabecera y las ocho muestras del bloque.

La Figura~\ref{fig:impl_payload_eeg_block_uv} representa la estructura lógica del formato de bloque de 8 muestras que el firmware entrega a Python

\begin{figure}[H]
    \centering
    \IfFileExists{images/uml/impl_payload_eeg_block_uv.png}{%
        \includegraphics[width=1\textwidth]{images/uml/impl_payload_eeg_block_uv.png}
    }{%
        \IfFileExists{images/uml/impl_payload_eeg_block_uv.pdf}{%
            \includegraphics[width=0.78\textwidth]{images/uml/impl_payload_eeg_block_uv.pdf}
        }{%
            \fbox{\parbox{0.9\textwidth}{\centering Diagrama pendiente de exportar desde Mermaid: \texttt{images/uml/impl\_payload\_eeg\_block\_uv.mmd}}}%
        }%
    }
    \caption{Estructura lógica del evento \texttt{eeg\_block\_uV}. El firmware transmite una cabecera seguida de ocho muestras; cada muestra conserva el estado del ADS1299 y cuatro canales en microvoltios.}
    \label{fig:impl_payload_eeg_block_uv}
\end{figure}


Con esta implementación, la relación entre firmware y backend Python queda definida de forma precisa. El microcontrolador entrega bloques de ocho muestras, con cuatro canales en microvoltios y estado ADS1299 por muestra. Python valida y almacena esos bloques antes de transformarlos en ventanas de análisis. La siguiente parte del capítulo describe cómo el backend consume esta señal ya estructurada para calcular características espectrales y métricas de calidad.


\subsection{Implementación del backend Python}

El backend Python constituye la segunda capa ejecutable de la cadena EEG--MIDI. Trabaja con bloques de señal ya reconstruidos, filtrados y escalados a microvoltios por el firmware. La frontera entre ambas capas queda definida por el evento \texttt{Bridge.notify("eeg\_block\_uV")}, que entrega al runtime Python bloques EEG con una estructura fija, como se describió en la sección anterior.

A partir de esos bloques, el backend realiza una cadena de procesamiento en varias etapas: primero valida y ordena los datos recibidos, después los almacena en una memoria temporal, extrae características espectrales de una ventana reciente y evalúa la calidad de esa ventana. El resultado de esta ruta interna queda disponible para dos usos distintos: por un lado, alimentar la sonificación descrita en la sección 6.3; por otro, publicar una vista resumida del estado del sistema para WebUI, capturas y validación.

La implementación se apoya en cuatro piezas principales. \texttt{EEGReceiver} recibe y valida los bloques procedentes del firmware. \texttt{EEGSignalProcessor} mantiene el buffer temporal multicanal y extrae ventanas recientes de señal. \texttt{DSPCore} concentra el cálculo espectral de CH1. Finalmente, \texttt{BackendService} coordina el ciclo de ejecución: drena la cola de recepción, actualiza características, evalúa calidad, actualiza el estado interno de sonificación y publica periódicamente el estado del backend. La Figura~\ref{fig:impl_backend_flujo_python} resume esta organización por capas.

\begin{figure}[H]
    \centering
    \IfFileExists{images/uml/impl_backend_flujo_python.png}{%
        \includegraphics[width=0.98\textwidth]{images/uml/impl_backend_flujo_python.png}
    }{%
        \IfFileExists{images/uml/impl_backend_flujo_python.pdf}{%
            \includegraphics[width=0.98\textwidth]{images/uml/impl_backend_flujo_python.pdf}
        }{%
            \fbox{\parbox{0.9\textwidth}{\centering Diagrama pendiente de exportar desde Mermaid: \texttt{images/uml/impl\_backend\_flujo\_python.mmd}}}%
        }%
    }
    \caption{Flujo principal del backend Python organizado por capas. La recepción valida el contrato \texttt{eeg\_block\_uV}, el procesador mantiene la ventana temporal, el bloque DSP/calidad produce características utilizables y \texttt{BackendService} coordina su uso por la sonificación y por los mecanismos de publicación.}

    \label{fig:impl_backend_flujo_python}
\end{figure}

\subsubsection{Recepción de muestras EEG}

La recepción en Python comienza cuando el firmware envía un bloque EEG mediante el evento \texttt{eeg\_block\_uV}. El microcontrolador no envía una muestra cada vez, sino paquetes de 8 muestras y 4 canales. Como la frecuencia de muestreo es de \SI{250}{\hertz}, cada bloque representa aproximadamente \SI{32}{\milli\second} de señal.

Para que Python pueda interpretar siempre esos bloques de la misma forma, el formato esperado se define en \texttt{eeg\_contract.py}. Este módulo recoge los valores comunes de la comunicación: frecuencia de muestreo, número de canales, tamaño del bloque y estructura del mensaje recibido. Así, el backend no depende de valores escritos de forma dispersa en distintos archivos.

La entrada real al backend se realiza mediante \texttt{EEGReceiver.eeg\_block\_uV()}. Este método se ejecuta automáticamente cuando llega un nuevo bloque desde Bridge. Su función es comprobar que el bloque tiene el formato esperado, verificar que los índices siguen una secuencia coherente y guardarlo en una cola interna. El cálculo espectral y la sonificación no se hacen en este punto, sino más adelante, cuando el bloque ya ha sido aceptado por el receptor.

Esta cola desacopla la recepción del procesamiento digital. El receptor solo valida y encola; posteriormente, el backend vacía la cola mediante \texttt{drain\_blocks\_to\_processor()} y entrega los bloques completos a \texttt{EEGSignalProcessor.add\_block\_uV()}. Con esta separación, una operación más costosa, como el cálculo espectral, no bloquea la llegada de nuevos eventos desde el firmware.

\subsubsection{Buffer temporal de señal}

\texttt{EEGSignalProcessor} transforma la sucesión de bloques recibidos en una señal temporal continua. Cada bloque llega en microvoltios y contiene 8 muestras consecutivas de los canales disponibles. Antes de escribir las muestras en memoria, el backend las convierte a voltios:

\[
x_{\mathrm{V}}[n,c] =
x_{\mu\mathrm{V}}[n,c]\cdot 10^{-6}.
\]

Esta conversión permite que el cálculo interno se realice en unidades del Sistema Internacional. Después, algunas métricas de diagnóstico se vuelven a expresar en microvoltios porque esa unidad resulta más interpretable para señales EEG.

La memoria principal de señal se implementa mediante un \textit{ring buffer} o buffer circular. Este buffer puede entenderse como un historial reciente de la señal. Las muestras nuevas se escriben una detrás de otra y, cuando se alcanza el final de la memoria reservada, la escritura continúa desde el principio, sobrescribiendo las muestras más antiguas. De esta forma, el backend no necesita desplazar toda la señal cada vez que llega un bloque nuevo.

El ring buffer y la ventana de análisis no son lo mismo. El ring buffer es la memoria continua donde se almacena la señal reciente; la ventana de análisis es un fragmento que se extrae de esa memoria para calcular características. En este sistema, la ventana tiene una duración de \SI{4}{\second}. Como la frecuencia de muestreo es de \SI{250}{\hertz}, cada ventana contiene 1000 muestras.

La PSD y las características espectrales se calculan sobre la ventana completa de \SI{4}{\second}, no solo sobre las muestras nuevas. Sin embargo, el backend no recalcula la PSD después de cada muestra individual. Espera a que hayan entrado 64 muestras nuevas, es decir, \SI{256}{\milli\second}, y entonces vuelve a extraer los últimos \SI{4}{\second} de señal desde el ring buffer. Por tanto, dos ventanas consecutivas se solapan: la nueva ventana conserva la mayor parte de la ventana anterior y añade las 64 muestras más recientes.

En términos prácticos, el flujo es el siguiente: los bloques EEG llegan cada \SI{32}{\milli\second}; cada bloque aporta 8 muestras; tras 8 bloques nuevos se acumulan 64 muestras nuevas; y en ese momento se recalculan las características espectrales usando de nuevo una ventana completa de 1000 muestras. Este salto entre dos cálculos consecutivos se denomina \textit{hop} y equivale a \SI{256}{\milli\second}. Estas escalas temporales se resumen en la Tabla~\ref{tab:impl_backend_tiempos}.

\begin{table}[!htbp]
    \centering
    \caption{Escalas temporales principales del backend Python.}
    \label{tab:impl_backend_tiempos}
    \small
    \renewcommand{\arraystretch}{1.12}
    \begin{tabular}{p{0.30\textwidth}p{0.22\textwidth}p{0.38\textwidth}}
        \hline
        \textbf{Elemento} & \textbf{Valor} & \textbf{Papel en el backend} \\
        \hline
        Bloque de entrada & 8 muestras, \SI{32}{\milli\second} & Unidad recibida mediante \texttt{eeg\_block\_uV}. \\
        Ventana de análisis & \SI{4}{\second}, 1000 muestras & Tramo usado para calcular características espectrales. \\
        Hop de features & 64 muestras, \SI{256}{\milli\second} & Actualización nominal de características EEG, aproximadamente \SI{3.9}{\hertz}. \\
        \hline
    \end{tabular}
\end{table}

Por tanto, el backend trabaja con dos escalas distintas: una escala corta de recepción, marcada por bloques de \SI{32}{\milli\second}, y una escala de análisis, basada en ventanas de \SI{4}{\second} que se actualizan cada \SI{256}{\milli\second}. Esta organización permite que el análisis espectral utilice suficiente contexto temporal sin recalcular la PSD después de cada muestra individual.

La Figura~\ref{fig:impl_backend_buffer_ventana} resume la relación entre bloque, buffer circular, ventana de análisis y hop de actualización.

\begin{figure}[!htbp]
    \centering
    \IfFileExists{images/uml/impl_backend_buffer_ventana.png}{%
        \includegraphics[width=0.50\textwidth]{images/uml/impl_backend_buffer_ventana.png}
    }{%
        \IfFileExists{images/uml/impl_backend_buffer_ventana.pdf}{%
            \includegraphics[width=0.98\textwidth]{images/uml/impl_backend_buffer_ventana.pdf}
        }{%
            \fbox{\parbox{0.9\textwidth}{\centering Diagrama pendiente de exportar desde Mermaid: \texttt{images/uml/impl\_backend\_buffer\_ventana.mmd}}}%
        }%
    }
    \caption{Organización temporal del buffer EEG en Python. Los bloques de ocho muestras se convierten a voltios, se escriben en un anillo multicanal y, al completarse la ventana de \SI{4}{\second}, CH1 se analiza cada 64 muestras.}
    \label{fig:impl_backend_buffer_ventana}
\end{figure}

\subsubsection{Extracción de características espectrales}

La base matemática de la densidad espectral de potencia, el cálculo de potencia por bandas y el método multitaper se ha introducido en Conceptos previos. Aquí se describe únicamente cómo se integra ese análisis en la ruta de ejecución del backend.

Cuando la ventana está completa y se cumple el salto de 64 muestras, \texttt{compute\_live\_features()} extrae la ventana reciente de CH1 y delega el análisis en \texttt{DSPCore}. Así, \texttt{EEGSignalProcessor} conserva la responsabilidad de memoria y selección temporal, mientras que \texttt{DSPCore} concentra el cálculo espectral de la señal unidimensional.

En la ruta en vivo se utiliza \texttt{psd\_method="multitaper"}. \texttt{DSPCore} preprocesa la ventana, estima la PSD, integra la potencia por bandas y devuelve un diccionario compacto de \textit{features}. La entrada al bloque de sonificacion y bloques posteriores no son los valores del espectro al completo. En su lugar, el backend mantiene un conjunto compacto de variables características de la señal. Estas variables se usan internamente para evaluar la calidad y actualizar los controles de sonificación, y también pueden publicarse de forma resumida para visualización, captura y validación.

La Tabla ~\ref{tab:impl_backend_features} resume el contenido general del diccionario generado por \texttt{DSPCore} a partir de la ventana de CH1 denominado \texttt{features}. Se actualiza cada vez que se recalcula la PSD, y resume aspectos clave de la distribución espectral de la señal.
\begin{table}[!htbp]
    \centering
    \caption{Resumen de características espectrales generadas por el backend.}
    \label{tab:impl_backend_features}
    \small
    \renewcommand{\arraystretch}{1.12}
    \begin{tabular}{p{0.30\textwidth}p{0.58\textwidth}}
        \hline
        \textbf{Grupo de salida} & \textbf{Información conservada} \\
        \hline
        Amplitud y pico global & Valor eficaz de la ventana y frecuencia de máxima potencia. Incluye \texttt{rms} y \texttt{peak\_freq}. \\
        Potencia por bandas & Potencias absolutas y relativas en delta, theta, alpha, beta y gamma, junto con los picos dominantes por banda. \\
        Descriptores derivados & Banda dominante y relación alpha/beta, usadas como resumen adicional de la distribución espectral. \\
        Uso posterior & Variables de entrada para calidad y sonificación; además se publican de forma resumida para visualización, captura y validación. \\
        \hline
    \end{tabular}
\end{table}



Además de las \textit{features}, el procesador genera \texttt{diagnostics}, que resume el estado observacional de la señal: amplitud en microvoltios, offset, dispersión, saturación, señal plana, saltos bruscos y presencia relativa de energía en torno a \SI{50}{\hertz}. A partir de \texttt{features}, \texttt{diagnostics}, métricas de recepción y disponibilidad de ventana, \texttt{spectral\_quality.py} calcula una puntuación, un estado y un factor de puerta. Esta capa no recalcula la PSD ni altera la señal; su papel es valorar la fiabilidad de la última ventana analizada antes de que sus características se usen en las etapas posteriores.

La Tabla~\ref{tab:impl_backend_diagnostics_quality} resume las tres salidas principales que intervienen en esta parte del backend: las características espectrales, los diagnósticos observacionales y la evaluación final de calidad.

\begin{table}[H]
\centering
\caption{Salidas principales de análisis y control de calidad.}
\label{tab:impl_backend_diagnostics_quality}
\small
\renewcommand{\arraystretch}{1.12}
\begin{tabular}{p{0.24\textwidth}p{0.30\textwidth}p{0.36\textwidth}}
    \hline
    \textbf{Bloque} & \textbf{Qué resume} & \textbf{Para qué se usa} \\
    \hline
    \texttt{features} & Contenido espectral de la ventana: RMS, picos y potencias por banda. & Alimentar la evaluación de calidad, la sonificación y el resumen publicable del análisis. \\
    \texttt{diagnostics} & Estado observacional de la señal: amplitud, offset, saturación, señal plana, saltos y ruido de red. & Detectar problemas de adquisición sin modificar el cálculo espectral. \\
    \texttt{spectral\_quality} & Puntuación, estado y factor de puerta derivados de \texttt{features}, \texttt{diagnostics} y recepción. & Determinar si la última ventana puede usarse con normalidad, con atenuación o con bloqueo. \\
    \hline
\end{tabular}
\end{table}


La Figura~\ref{fig:impl_backend_features_quality} resume esta separación entre cálculo espectral, diagnóstico observacional y evaluación de calidad.

\begin{figure}[H]
    \centering
    \IfFileExists{images/uml/impl_backend_features_quality.png}{%
        \includegraphics[width=0.98\textwidth]{images/uml/impl_backend_features_quality.png}
    }{%
        \IfFileExists{images/uml/impl_backend_features_quality.pdf}{%
            \includegraphics[width=0.98\textwidth]{images/uml/impl_backend_features_quality.pdf}
        }{%
            \fbox{\parbox{0.9\textwidth}{\centering Diagrama pendiente de exportar desde Mermaid: \texttt{images/uml/impl\_backend\_features\_quality.mmd}}}%
        }%
    }
    \caption{Separación entre extracción espectral, diagnóstico observacional y evaluación de calidad en el backend Python. La ventana CH1 alimenta \texttt{DSPCore}; las características y diagnósticos se combinan después en \texttt{spectral\_quality} antes de pasar a la sonificacion o publicarse en el \textit{snapshot}.}
    \label{fig:impl_backend_features_quality}
\end{figure}

\subsubsection{Publicación de estado y métricas}

Las etapas anteriores describen la ruta operativa del backend: recepción de bloques, actualización del buffer, cálculo de características, evaluación de calidad y actualización de los controles internos de sonificación. Sobre esa ruta, \texttt{BackendService} construye periódicamente un \textit{snapshot}, siendo este una vista estructurada del estado actual del sistema.

El \textit{snapshot} permite observar y registrar el funcionamiento del backend sin acceder directamente a sus objetos internos. En lugar de consultar por separado el receptor, el buffer, el núcleo DSP, el evaluador de calidad o el motor musical, la WebUI, las capturas, los registros en disco y las herramientas de validación leen una estructura común con los últimos valores disponibles.

El estado principal del runtime se publica dentro de \texttt{status.state}. Este campo describe la fase de ejecución del backend: espera de datos, acumulación de ventana, recepción activa o disponibilidad de características. Debe distinguirse de \texttt{spectral\_quality.state}, que resume la fiabilidad de la última ventana analizada. La Figura~\ref{fig:impl_backend_runtime_states} muestra esta separación entre estado de ejecución y estado de calidad.

\begin{figure}[!htbp]
\centering
\IfFileExists{images/uml/impl_backend_runtime_states.png}{%
\includegraphics[width=0.96\textwidth]{images/uml/impl_backend_runtime_states.png}
}{%
\IfFileExists{images/uml/impl_backend_runtime_states.pdf}{%
\includegraphics[width=0.96\textwidth]{images/uml/impl_backend_runtime_states.pdf}
}{%
\fbox{\parbox{0.9\textwidth}{\centering Diagrama pendiente de exportar desde Mermaid: \texttt{images/uml/impl_backend_runtime_states.mmd}}}%
}%
}
\caption{Separación entre estado de ejecución y estado de calidad en el backend Python. \texttt{status.state} describe la fase del runtime, mientras que \texttt{spectral\_quality.state} resume la fiabilidad de la última ventana analizada.}
\label{fig:impl_backend_runtime_states}
\end{figure}


Además del estado de ejecución y de calidad, el \textit{snapshot} agrupa configuración activa, métricas de recepción, características resumidas, diagnósticos, estado de captura, rendimiento, errores y un resumen del estado musical y MIDI. De esta forma, la publicación no repite el procesado de señal, sino que recopila los últimos resultados calculados por la ruta interna del backend.

La Figura~\ref{fig:impl_backend_snapshot_schema} resume esta organización del \textit{snapshot} como vista común de consulta.

\begin{figure}[H]
\centering
\IfFileExists{images/uml/impl_backend_snapshot_schema.png}{%
\includegraphics[width=0.88\textwidth]{images/uml/impl_backend_snapshot_schema.png}
}{%
\IfFileExists{images/uml/impl_backend_snapshot_schema.pdf}{%
\includegraphics[width=0.98\textwidth]{images/uml/impl_backend_snapshot_schema.pdf}
}{%
\fbox{\parbox{0.9\textwidth}{\centering Diagrama pendiente de exportar desde Mermaid: \texttt{images/uml/impl_backend_snapshot_schema.mmd}}}%
}%
}
\caption{Estructura del \textit{snapshot} publicado por el backend. \texttt{BackendService.\_build\_snapshot()} agrupa los últimos valores disponibles de configuración, estado, recepción, características, diagnósticos, calidad, captura, rendimiento, errores y estado musical en una vista común para observación y validación.}
\label{fig:impl_backend_snapshot_schema}
\end{figure}

La Tabla~\ref{tab:impl_backend_snapshot} recoge los grupos principales del \textit{snapshot}. La agrupación por bloques mantiene la trazabilidad técnica sin listar cada clave interna del diccionario.

\begin{table}[!htbp]
\centering
\caption{Grupos principales del \textit{snapshot} publicado por el backend Python.}
\label{tab:impl_backend_snapshot}
\small
\renewcommand{\arraystretch}{1.12}
\begin{tabular}{>{\raggedright\arraybackslash}p{0.25\textwidth}>{\raggedright\arraybackslash}p{0.30\textwidth}>{\raggedright\arraybackslash}p{0.35\textwidth}}
\hline
\textbf{Grupo} & \textbf{Contenido} & \textbf{Finalidad} \\
\hline
\texttt{config/status} & Configuración activa y fase del runtime. & Indicar con qué parámetros trabaja el backend y en qué fase de ejecución se encuentra. \\
\texttt{rx} & Tasas de recepción, pérdidas, cola, bloques malformados y estados inválidos. & Vigilar la estabilidad de la comunicación MCU--Python. \\
\texttt{features/diagnostics} & Resumen espectral y métricas observacionales de la ventana reciente. & Mostrar el estado de la señal sin publicar el espectro completo\linebreak ni el buffer EEG. \\
\texttt{spectral\_quality} & Puntuación, estado, factor de puerta y validez para sonificación. & Registrar el criterio de calidad aplicado a la última ventana analizada. \\
\texttt{sonification/music/midi} & Controles musicales, notas recientes, estado del scheduler y transporte MIDI. & Observar el resultado musical generado a partir del estado interno del backend. \\
\texttt{capture/errors} & Estado de captura y contadores de errores. & Facilitar validación experimental y depuración. \\
\hline
\end{tabular}
\end{table}


La publicación del \textit{snapshot} se realiza con el periodo \SI{0.2}{\second}, equivalente a una frecuencia aproximada de \SI{5}{\hertz}. Esta cadencia corresponde a la actualización de la vista pública del estado. El cálculo espectral sigue el \textit{hop} de análisis descrito anteriormente, y la generación musical sigue la duración de compás definida en la etapa de sonificación.

Con esta organización, el backend Python queda delimitado como una capa de recepción, memoria temporal, extracción espectral, diagnóstico observacional y publicación de estado. La sonificación utiliza las características y la calidad calculadas por la ruta interna del backend, mientras que el \textit{snapshot} ofrece una vista resumida de ese mismo estado para visualización, captura, registro y validación.


\subsection{Implementación de la sonificación}

La sonificación constituye la tercera capa ejecutable de la cadena EEG--MIDI. En las secciones anteriores se ha descrito cómo el firmware adquiere la señal EEG real y cómo el backend Python la transforma en una ventana reciente de características espectrales y métricas de calidad. A partir de ese punto, la sonificación utiliza el estado de las señal EEG resumido por el backend en el diccionario de características espectrales para generar música en tiempo real.

La función de esta capa es transformar una representación compacta de la señal en controles musicales. Estos controles son valores normalizados que indican, por ejemplo, cuánta actividad, tensión, densidad rítmica, estabilidad, registro o dinámica debe tener la salida musical. Por tanto, el sistema no realiza una traducción literal del tipo ``el valor de la potencia de una banda EEG equivale a una nota concreta''. En su lugar, las bandas EEG, el RMS, la frecuencia pico y la calidad de la ventana modifican parámetros musicales de alto nivel.

La generación musical mantiene separadas dos fuentes de información. Por un lado, el EEG aporta controles variables en el tiempo. Por otro lado, la configuración musical fija el marco sobre el que se genera la música: escala, nota raíz, nota principal, canal MIDI, programa instrumental y duración del compás. Esta separación evita interpretar la sonificación como una lectura clínica directa de la señal y permite entenderla como un mapeo entre características EEG y comportamiento musical.

El flujo completo puede resumirse así: las características EEG y el estado de calidad se adaptan mediante \texttt{SonificationFeatureAdapter}; el resultado se almacena en una estructura \texttt{SonificationFeatures}; después se construye un segmento musical, se genera un compás, se crean notas y finalmente esas notas se programan como eventos MIDI. En la última etapa, los eventos se convierten en bytes MIDI y se envían al firmware mediante \texttt{Bridge.call("midi\_bytes")}, desde donde salen por la UART asociada al conector MIDI físico.

La Figura~\ref{fig:impl_sonificacion_flujo} resume esta ruta. Debe leerse como continuación directa de la sección 6.2: el bloque de procesamiento ya ha calculado las características y la calidad de la ventana, y la sonificación empieza cuando ese estado interno se convierte en controles musicales.

\begin{figure}[H]
    \centering
    \IfFileExists{images/uml/impl_sonificacion_flujo.png}{%
        \includegraphics[width=0.98\textwidth,height=0.42\textheight,keepaspectratio]{images/uml/impl_sonificacion_flujo.png}
    }{%
        \IfFileExists{images/uml/impl_sonificacion_flujo.pdf}{%
            \includegraphics[width=0.98\textwidth,height=0.42\textheight,keepaspectratio]{images/uml/impl_sonificacion_flujo.pdf}
        }{%
            \fbox{\parbox{0.9\textwidth}{\centering Diagrama pendiente de exportar desde Mermaid: \texttt{images/uml/impl\_sonificacion\_flujo.mmd}}}%
        }%
    }
    \caption{Flujo principal de sonificación implementado. El estado EEG calculado por el backend se transforma primero en controles musicales; después se genera una estructura musical formada por segmento, compás y notas; finalmente, las notas se programan como eventos MIDI, se convierten a bytes y se envían al firmware mediante \texttt{midi\_bytes}.}
    \label{fig:impl_sonificacion_flujo}
\end{figure}


\subsubsection{Adaptación de características EEG a controles musicales}

El primer paso de la sonificación consiste en transformar las características calculadas por el backend en variables musicales manejables. Para ello, el módulo \texttt{sonification\_features.py} recibe el diccionario producido por \texttt{compute\_live\_features()} y lo adapta a una estructura específica denominada \texttt{SonificationFeatures}. Esta estructura actúa como puente entre el análisis EEG y el motor musical.

La idea principal es que las características espectrales no se usan directamente como notas. Primero se convierten en controles normalizados en el intervalo \([0,1]\). Un valor cercano a 0 indica poca influencia de ese control y un valor cercano a 1 indica una influencia mayor. De esta forma, magnitudes como potencia relativa de bandas, RMS, frecuencia pico y calidad de ventana pasan a expresarse como controles de ritmo, actividad, registro, armonía, dinámica y probabilidad de nota.

Para describir el cálculo se utiliza la siguiente notación. Las variables \(\theta\), \(\alpha\), \(\beta\) y \(\gamma\) representan las potencias relativas de las bandas EEG; \(r\) representa el RMS normalizado; \(f_n\) es la frecuencia pico normalizada; y \(C(x)\) indica que el valor se acota al intervalo \([0,1]\):

\[
    C(x)=\min(1,\max(0,x))
\]

También se define la proporción de beta dentro del conjunto alfa--beta:

\[
    \beta_d = C\left(\frac{\beta}{\alpha+\beta}\right)
\]

Las Tablas~\ref{tab:impl_sonificacion_controles_ritmo} y~\ref{tab:impl_sonificacion_controles_melodicos} resumen la conversión. En cada fila se indica de forma compacta cómo se obtiene el control y qué efecto tiene cuando aumenta. Estas expresiones definen el mapeo utilizado por el prototipo para convertir características EEG en comportamiento musical.

\begin{table}[H]
    \centering
    \caption{Controles asociados a ritmo, actividad y dinámica.}
    \label{tab:impl_sonificacion_controles_ritmo}
    \footnotesize
    \setlength{\tabcolsep}{3pt}
    \renewcommand{\arraystretch}{1.10}
    \begin{tabular}{p{0.24\textwidth}p{0.31\textwidth}p{0.36\textwidth}}
        \hline
        \textbf{Control} & \textbf{Cómo se obtiene} & \textbf{Efecto cuando aumenta} \\
        \hline

        \texttt{beta\_gamma\_drive} &
        \(C(0.75\,\beta_d + 0.25\,\gamma)\). Crece cuando beta domina frente a alfa y aumenta gamma. &
        Aumenta la tensión musical. Favorece síncopas, desplazamientos rítmicos y cambios armónicos menos reposados. \\

        \texttt{rms\_beta\_activity} &
        \(C(0.45\,r + 0.35\,\beta + 0.20\,\gamma)\). Combina intensidad de señal y bandas rápidas. &
        Aumenta la actividad global del compás. El patrón rítmico tiende a tener más presencia e intensidad. \\

        \texttt{band\_driven\_density} &
        \(C(0.55\,\beta + 0.20\,\gamma + 0.15\,r + 0.10(1-\texttt{alpha\_drive}))\). &
        Controla la densidad rítmica. Valores bajos producen menos notas; valores altos activan más posiciones del compás. \\

        \texttt{rms\_band\_velocity} &
        \(C(0.30 + 0.70(0.65\,r + 0.25\,\beta + 0.10\,\gamma))\). &
        Controla la intensidad de las notas. Valores altos producen mayor \textit{velocity} MIDI; valores bajos producen notas más suaves. \\

        \texttt{band\_note\_probability} &
        \(C(0.15 + 0.80\,\texttt{band\_driven\_density})\). Depende principalmente de la densidad calculada. &
        Aumenta la probabilidad de que una posición rítmica seleccionada termine generando una nota. \\
        \hline
    \end{tabular}
\end{table}

\begin{table}[H]
    \centering
    \caption{Controles asociados a registro, armonía y fiabilidad.}
    \label{tab:impl_sonificacion_controles_melodicos}
    \footnotesize
    \setlength{\tabcolsep}{3pt}
    \renewcommand{\arraystretch}{1.10}
    \begin{tabular}{p{0.24\textwidth}p{0.31\textwidth}p{0.36\textwidth}}
        \hline
        \textbf{Control} & \textbf{Cómo se obtiene} & \textbf{Efecto cuando aumenta} \\
        \hline

        \texttt{alpha\_drive} &
        \(C\left(\dfrac{\alpha}{\alpha+\beta}\right)\). Crece cuando alfa domina frente a beta. &
        Actúa como contrapeso de las bandas rápidas. Favorece una salida menos densa y más estable armónicamente. \\

        \texttt{spectral\_register} &
        \(C(0.15 + 0.45\,\beta_d + 0.25\,\gamma + 0.15\,f_n)\). Combina beta, gamma y frecuencia pico normalizada. &
        Desplaza el centro de las notas. Valores bajos llevan la melodía hacia registros graves; valores altos hacia registros agudos. \\

        \texttt{alpha\_stability} &
        \(C(0.75\,\texttt{alpha\_drive} + 0.15\,\theta + 0.10(1-r))\). &
        Influye en la elección de acordes. Valores altos favorecen acordes más estables; valores bajos permiten mayor tensión armónica. \\

        \texttt{quality\_gate} &
        Factor \(q_g \in [0,1]\) procedente de la calidad de la ventana. &
        Regula la fiabilidad de la sonificación. Puede dejar pasar los controles, atenuarlos o bloquear nuevo material musical. \\
        \hline
    \end{tabular}
\end{table}

Esta organización permite leer cada control de izquierda a derecha: primero se identifica qué combinación de características EEG lo produce y después se ve qué decisión musical modifica. Por ejemplo, \texttt{band\_driven\_density} no genera notas por sí mismo, sino que se transforma después en una cadencia rítmica discreta; \texttt{spectral\_register} no fija una nota concreta, sino que desplaza el centro alrededor del cual se elegirán las alturas; y \texttt{rms\_band\_velocity} no cambia el ritmo, sino la intensidad MIDI de las notas.

En la siguiente etapa, estos controles se agrupan en un \texttt{MusicSegment}. En esa estructura, \texttt{band\_driven\_density} se convierte en una cadencia rítmica \texttt{LOW}, \texttt{MEDIUM} o \texttt{HIGH}; \texttt{spectral\_register} pasa a ser una pista de registro; \texttt{alpha\_stability} se usa como estabilidad armónica; \texttt{beta\_gamma\_drive} se usa como tensión; y \texttt{rms\_band\_velocity} y \texttt{band\_note\_probability} se conservan para modular notas y dinámica.

Sobre estos controles se aplica el \texttt{quality\_gate}. Este factor regula cuánto pueden influir las características EEG sobre la salida musical. Cuando la calidad es alta, los controles se usan con normalidad. Si la ventana presenta artefactos o baja fiabilidad, se atenúan variables sensibles como actividad, densidad, velocidad y probabilidad de nota. Si la ventana no es válida, el sistema evita generar nuevo material musical a partir de ella.

La Figura~\ref{fig:impl_sonificacion_quality_gate} muestra esta relación entre características, diagnósticos, calidad y controles musicales. La calidad actúa como una puerta entre el estado EEG calculado por el backend y el motor musical.

\begin{figure}[H]
    \centering
    \IfFileExists{images/uml/impl_sonificacion_quality_gate.png}{%
        \includegraphics[width=0.95\textwidth,height=0.42\textheight,keepaspectratio]{images/uml/impl_sonificacion_quality_gate.png}
    }{%
        \IfFileExists{images/uml/impl_sonificacion_quality_gate.pdf}{%
            \includegraphics[width=0.95\textwidth,height=0.42\textheight,keepaspectratio]{images/uml/impl_sonificacion_quality_gate.pdf}
        }{%
            \fbox{\parbox{0.9\textwidth}{\centering Diagrama pendiente de exportar desde Mermaid: \texttt{images/uml/impl\_sonificacion\_quality\_gate.mmd}}}%
        }%
    }
    \caption{Papel del \textit{quality gate} en la sonificación. La calidad de la ventana decide si los controles se aplican plenamente, se atenúan o se evita generar nuevo material musical a partir de una ventana no fiable.}
    \label{fig:impl_sonificacion_quality_gate}
\end{figure}

Finalmente, \texttt{SonificationFeatureAdapter} introduce una memoria temporal ligera. El RMS se normaliza respecto a una línea base adaptativa y varios controles se suavizan mediante una media exponencial. Esta memoria evita que pequeñas variaciones entre ventanas consecutivas produzcan saltos musicales bruscos, manteniendo a la vez los metadatos de calidad y el estado \texttt{valid} asociados a cada actualización.


\subsubsection{Construcción de segmentos musicales}

Una vez obtenidos los controles de sonificación, el backend construye un estado musical intermedio mediante \texttt{MusicSegmentBuilder}. Esta etapa recibe los controles ya suavizados, la escala activa, la nota principal y el tiempo musical actual. El resultado es un objeto \texttt{MusicSegment}, que no contiene todavía notas individuales, sino la información necesaria para generar el siguiente compás.

La función de \texttt{MusicSegmentBuilder} es traducir los controles continuos de 6.3.1 a campos musicales más directamente utilizables. Por ejemplo, la densidad rítmica continua se convierte en una cadencia discreta, el registro espectral se guarda como pista de registro, la estabilidad pasa a ser un criterio armónico y la dinámica se conserva como factor de \textit{velocity}. De este modo, el sistema separa dos pasos: primero calcula controles a partir del EEG y después organiza esos controles como estado musical.

La Figura~\ref{fig:impl_sonificacion_music_segment_builder} muestra esta reorganización. El diagrama debe leerse de izquierda a derecha: los controles de \texttt{SonificationFeatures} entran en \texttt{MusicSegmentBuilder}; el builder los transforma o copia a campos musicales concretos; y el resultado queda agrupado en \texttt{MusicSegment}, que será la entrada del generador de compases.

\begin{figure}[H]
    \centering
    \IfFileExists{images/uml/impl_sonificacion_music_segment_builder.png}{%
        \includegraphics[width=0.98\textwidth,height=0.48\textheight,keepaspectratio]{images/uml/impl_sonificacion_music_segment_builder.png}
    }{%
        \IfFileExists{images/uml/impl_sonificacion_music_segment_builder.pdf}{%
            \includegraphics[width=0.98\textwidth,height=0.48\textheight,keepaspectratio]{images/uml/impl_sonificacion_music_segment_builder.pdf}
        }{%
            \fbox{\parbox{0.9\textwidth}{\centering Diagrama pendiente de exportar desde Mermaid: \texttt{images/uml/impl\_sonificacion\_music\_segment\_builder.mmd}}}%
        }%
    }
    \caption{Construcción del \texttt{MusicSegment}. Los controles de sonificación se reorganizan en campos musicales: cadencia rítmica, registro, tensión, estabilidad, actividad, dinámica y probabilidad de nota. La escala, la nota principal y la duración proceden de la configuración musical.}
    \label{fig:impl_sonificacion_music_segment_builder}
\end{figure}

La transformación más importante de esta etapa es la conversión de \texttt{band\_driven\_density} en \texttt{rhythm\_cadence}. El valor de densidad es continuo, pero el generador de compases trabaja con tres niveles discretos: \texttt{LOW}, \texttt{MEDIUM} y \texttt{HIGH}. Una cadencia baja tiende a compases con menos notas; una cadencia media produce un patrón intermedio; y una cadencia alta permite compases con más posiciones activas. Para evitar saltos constantes entre estados, el cambio de cadencia se realiza con histéresis: una pequeña variación entre ventanas consecutivas no provoca necesariamente un cambio de ritmo.

La Tabla~\ref{tab:impl_music_segment_mapeo} resume los campos principales de \texttt{MusicSegment}. En la columna central se indica qué entrada usa el builder, evitando expresiones ambiguas como ``derivado de''. La última columna indica para qué se usará ese campo en las etapas posteriores.

\begin{table}[H]
    \centering
    \caption{Mapa de entradas y campos principales de \texttt{MusicSegment}.}
    \label{tab:impl_music_segment_mapeo}
    \footnotesize
    \setlength{\tabcolsep}{3pt}
    \renewcommand{\arraystretch}{1.10}
    \begin{tabular}{p{0.27\textwidth}p{0.31\textwidth}p{0.33\textwidth}}
        \hline
        \textbf{Campo en \texttt{MusicSegment}} & \textbf{Entrada usada por el builder} & \textbf{Uso posterior} \\
        \hline
        \texttt{rhythm\_cadence} &
        \texttt{band\_driven\_density} convertida a \texttt{LOW}, \texttt{MEDIUM} o \texttt{HIGH}. &
        Define la cantidad base de posiciones activas del compás. \\
        \texttt{register\_hint} &
        Valor de \texttt{spectral\_register}. &
        Desplaza el centro melódico hacia registros graves o agudos. \\
        \texttt{tension} &
        Valor de \texttt{beta\_gamma\_drive}. &
        Favorece síncopas, movimiento rítmico y mayor tensión melódica o armónica. \\
        \texttt{harmonic\_stability} &
        Valor de \texttt{alpha\_stability}. &
        Ayuda a elegir acordes más estables o más tensos. \\
        \texttt{activity} &
        Valor de \texttt{rms\_beta\_activity}. &
        Ajusta la presencia general del compás y puede aumentar ligeramente los eventos. \\
        \texttt{velocity\_factor} &
        Valor de \texttt{rms\_band\_velocity}. &
        Modula la intensidad de las notas MIDI. \\
        \texttt{note\_probability} &
        Valor de \texttt{band\_note\_probability}. &
        Ajusta la probabilidad de que una posición rítmica genere una nota. \\
        \texttt{scale} y \texttt{main\_note\_midi} &
        Escala y nota principal configuradas en el sistema. &
        Mantienen el marco tonal y el centro musical de la melodía. \\
        \hline
    \end{tabular}
\end{table}

El segmento musical también almacena el intervalo temporal del siguiente compás. En la ruta en tiempo real, esta duración viene dada por \texttt{MUSIC\_BAR\_SEC}, cuyo valor por defecto es \SI{2}{\second}. Por tanto, cada \texttt{MusicSegment} representa el estado musical que se usará para generar un compás de esa duración, asociado al estado EEG reciente.

Finalmente, el campo \texttt{valid} permite mantener la relación con la calidad de la sonificación. Si el estado anterior no es válido, el backend no utiliza ese resultado para generar nuevo material musical. Así, \texttt{MusicSegment} funciona como una frontera clara entre los controles continuos y la generación de compases: cuando el estado es válido, el objeto se entrega a \texttt{BarGenerator}; cuando no lo es, la generación se bloquea hasta recibir una actualización fiable.

En resumen, \texttt{MusicSegmentBuilder} convierte los controles de sonificación en una descripción musical compacta del siguiente compás. A partir de ese momento, el sistema ya dispone de escala, nota principal, cadencia rítmica, registro, tensión, estabilidad, actividad, dinámica y probabilidad de nota. El siguiente apartado explica cómo \texttt{BarGenerator} usa esta información para construir el compás propiamente dicho.

\subsubsection{Generación de compases}

Una vez construido el \texttt{MusicSegment}, el siguiente paso consiste en generar el compás que servirá como base para las notas. En esta implementación, un compás se entiende como una plantilla temporal. Su duración se divide en 16 posiciones: cuatro pulsos y cuatro subdivisiones por pulso. Las posiciones marcadas indican puntos donde el sistema podrá colocar una nota; las posiciones vacías representan silencios.

La clase encargada de esta etapa es \texttt{BarGenerator}. Su entrada es un \texttt{MusicSegment} ya validado y su salida es un objeto \texttt{Bar}. Este objeto resume tres decisiones principales: qué acorde servirá como referencia, en qué posiciones del compás podrán aparecer notas y qué acento tendrá cada posición. La nota concreta que sonará en cada posición marcada se decide después en \texttt{NoteGenerator}.

La primera decisión es elegir el acorde de referencia del compás. Para ello, \texttt{BarGenerator} usa la escala configurada y dos controles procedentes del segmento: la estabilidad armónica y la tensión. Cuando la estabilidad es alta y la tensión es baja, el sistema tiende a un acorde más estable. Cuando la estabilidad baja o la tensión aumenta, el sistema permite un acorde con más sensación de movimiento. Para que el acorde no cambie continuamente por pequeñas variaciones de la señal EEG, el cambio se realiza con un margen de seguridad, implementado como histéresis.

La segunda decisión es definir cuántas posiciones del compás estarán disponibles para generar notas. Esta decisión parte de \texttt{rhythm\_cadence}, que puede tomar los valores \texttt{LOW}, \texttt{MEDIUM} o \texttt{HIGH}. Una cadencia baja genera un compás con pocas posiciones marcadas; una cadencia media genera un patrón intermedio; y una cadencia alta permite un compás más denso. Después, el control de actividad ajusta ligeramente esa cantidad: si la actividad es alta, el compás puede ganar alguna posición adicional; si es baja, puede reducirse.

El patrón rítmico se construye sobre las 16 posiciones del compás. La primera posición se marca siempre para que el compás tenga un punto de apoyo claro. El resto de posiciones se selecciona mediante pesos. El sistema favorece el inicio del compás y los pulsos principales, pero también puede activar posiciones intermedias. Los controles EEG modifican esa selección: la actividad aumenta la presencia general del patrón, la tensión favorece posiciones desplazadas u \textit{offbeats}, y \texttt{note\_probability} regula la probabilidad global de que aparezcan notas.

Además del patrón de posiciones, \texttt{BarGenerator} calcula un mapa de acentos. En música, un acento indica que una nota o una posición del compás debe destacar más que otra, normalmente porque se interpreta con mayor intensidad. En el código, este mapa se almacena en \texttt{amplitude\_slots}. Cada posición del compás tiene asociado un valor entre 0 y 1: un valor alto indica que, si después se genera una nota en esa posición, tenderá a sonar con más fuerza; un valor bajo indica que sonará de forma más suave.

Este mapa parte de una regla musical fija: el inicio del compás recibe más acento, los pulsos principales reciben un acento intermedio y otras posiciones reciben menos acento. Después, en la generación de notas, las posiciones activas se ajustan con \texttt{velocity\_factor}, mientras que \texttt{activity} eleva ligeramente el nivel general del mapa.


La Tabla~\ref{tab:impl_bar_generator_bar} resume los campos principales del objeto \texttt{Bar}. La tabla debe leerse como la salida de esta etapa: el compás ya tiene una referencia armónica, un patrón rítmico y un mapa de acentos, pero las notas concretas se generan en la etapa siguiente.

\begin{table}[H]
    \centering
    \caption{Información generada por \texttt{BarGenerator} en el objeto \texttt{Bar}.}
    \label{tab:impl_bar_generator_bar}
    \footnotesize
    \setlength{\tabcolsep}{3pt}
    \renewcommand{\arraystretch}{1.10}
    \begin{tabular}{p{0.27\textwidth}p{0.31\textwidth}p{0.33\textwidth}}
        \hline
        \textbf{Campo en \texttt{Bar}} & \textbf{Cómo se obtiene} & \textbf{Papel en la generación musical} \\
        \hline
        \texttt{index} &
        Índice del compás generado. &
        Permite ordenar los compases dentro de la secuencia musical. \\
        \texttt{t\_start} y \texttt{t\_end} &
        Intervalo temporal heredado de \texttt{MusicSegment}. &
        Define cuándo empieza y termina el compás. \\
        \texttt{chord\_root\_midi} &
        Nota base del acorde elegido dentro de la escala. &
        Sirve como referencia armónica principal del compás. \\
        \texttt{chord\_pitches} &
        Conjunto de notas que forman el acorde del compás. &
        Proporciona las notas de apoyo para la melodía posterior. \\
        \texttt{note\_positions} &
        Patrón de 16 posiciones marcadas o vacías. &
        Indica en qué partes del compás podrán aparecer notas. \\
        \texttt{stability} &
        Valor de estabilidad usado al elegir el acorde. &
        Conserva la relación entre el estado EEG y la decisión armónica. \\
        \texttt{amplitude\_slots} &
        Mapa de acentos con base musical fija, ajustado por \texttt{velocity\_factor} y ligeramente elevado por \texttt{activity}. &
        Indica qué posiciones del compás tenderán a sonar con mayor o menor intensidad si generan una nota. \\
        \hline
    \end{tabular}
\end{table}





En resumen, \texttt{BarGenerator} convierte el estado musical del segmento en una plantilla de compás. Esta plantilla no contiene todavía notas finales, sino tres elementos que serán usados por la etapa siguiente: una referencia armónica, un patrón de posiciones donde podrán aparecer notas y un mapa de acentos. A partir de esa estructura, \texttt{NoteGenerator} decidirá qué nota concreta se coloca en cada posición activa, con qué duración y con qué \textit{velocity} MIDI.


\subsubsection{Generación de notas}

Una vez generado el compás, el siguiente paso consiste en convertir su plantilla en notas concretas. La clase encargada de esta etapa es \texttt{NoteGenerator}. Su entrada está formada por dos objetos: el \texttt{MusicSegment}, que conserva los controles musicales derivados del EEG, y el \texttt{Bar}, que contiene el acorde, las posiciones marcadas y el mapa de acentos del compás. La salida es una lista de objetos \texttt{NoteEvent}.

La idea principal es que el compás define cuándo puede sonar algo, mientras que \texttt{NoteGenerator} decide qué nota suena en cada posición marcada, cuánto dura y con qué intensidad MIDI. Por tanto, esta etapa transforma una plantilla rítmica y armónica en eventos musicales individuales. Cada \texttt{NoteEvent} contiene el instante de inicio, el instante de fin, la nota MIDI, la \textit{velocity}, el canal y el programa instrumental.

La Figura~\ref{fig:impl_sonificacion_segmento_compas_nota} resume esta relación. El \texttt{MusicSegment} conserva los controles derivados del EEG; el \texttt{Bar} aporta la plantilla de compás; y \texttt{NoteGenerator} completa esa plantilla generando eventos \texttt{NoteEvent}.


\begin{figure}[H]
    \centering
    \IfFileExists{images/uml/impl_sonificacion_segmento_compas_nota.png}{%
        \includegraphics[width=0.98\textwidth,height=0.46\textheight,keepaspectratio]{images/uml/impl_sonificacion_segmento_compas_nota.png}
    }{%
        \IfFileExists{images/uml/impl_sonificacion_segmento_compas_nota.pdf}{%
            \includegraphics[width=0.98\textwidth,height=0.46\textheight,keepaspectratio]{images/uml/impl_sonificacion_segmento_compas_nota.pdf}
        }{%
            \fbox{\parbox{0.9\textwidth}{\centering Diagrama pendiente de exportar desde Mermaid: \texttt{images/uml/impl\_sonificacion\_segmento\_compas\_nota.mmd}}}%
        }%
    }
    \caption{Relación entre segmento musical, compás y notas. El \texttt{MusicSegment} aporta los controles musicales derivados del EEG; el \texttt{Bar} define acorde, posiciones activas y acentos; y \texttt{NoteGenerator} genera una lista de eventos \texttt{NoteEvent}.}
    \label{fig:impl_sonificacion_segmento_compas_nota}
\end{figure}

El generador recorre las 16 posiciones del compás, pero solo actúa sobre las posiciones marcadas en \texttt{note\_positions}. Las posiciones vacías se omiten y producen silencio. Para cada posición activa se calcula primero su intervalo temporal. La nota empieza en la posición correspondiente del compás y termina en la siguiente posición activa. Si no existe otra posición marcada después, la nota se mantiene hasta el final del compás. Esta regla garantiza que cada nota tenga un inicio y un final definidos antes de pasar al scheduler MIDI.

Después se decide qué nota MIDI debe sonar. El sistema no elige cualquier nota posible, sino notas pertenecientes a la escala configurada. Primero se calcula un centro melódico alrededor de la nota principal. Este centro se desplaza mediante \texttt{register\_hint}: valores bajos desplazan la melodía hacia registros más graves, valores intermedios la mantienen cerca de la nota principal y valores altos la llevan hacia registros más agudos. Así, el EEG puede modificar el registro de la melodía sin cambiar la escala ni la nota principal configurada.

A partir de ese centro, \texttt{NoteGenerator} construye una lista de notas candidatas dentro de la escala. Estas candidatas se separan en dos grupos. Las notas de acorde son las que pertenecen al acorde elegido para el compás y funcionan como puntos de apoyo. Las notas de paso son otras notas de la escala que permiten movimiento entre esos puntos de apoyo. De esta forma, las posiciones principales del compás tienden a usar notas del acorde, mientras que las posiciones intermedias pueden usar notas de paso cuando existen candidatas adecuadas.

La señal EEG también influye en la elección de la nota. El control \texttt{tension} desplaza el objetivo melódico: con mayor tensión, el sistema tiende a explorar notas algo más altas dentro del rango permitido. Además, la actividad, la tensión y \texttt{note\_probability} aumentan la variedad de candidatos, evitando que la melodía repita siempre las mismas notas. Aun así, el generador conserva continuidad melódica mediante una memoria de la nota anterior. Si una nota candidata queda demasiado lejos de la anterior, se acerca por octavas o se sustituye por una nota más centrada. Con tensión baja se favorecen saltos pequeños; con tensión alta se permiten saltos algo mayores.

La intensidad final de cada nota se calcula mediante la \textit{velocity} MIDI. Se combina el mapa de acentos almacenado en \texttt{amplitude\_slots}, generado en el compás, con el control EEG \texttt{velocity\_factor}. El valor \texttt{velocity\_factor} permite que la señal EEG aumente o reduzca la intensidad final. El resultado se acota entre una velocidad mínima y máxima para evitar notas demasiado débiles o excesivamente fuertes.

En el primer slot del compás, el sistema puede generar el acorde completo como marcador armónico. Esto significa que, en lugar de crear una sola nota, puede crear varias notas simultáneas pertenecientes al acorde. Este acorde completo no se reproduce en todos los compases, sino solo en situaciones concretas: al inicio de la generación, cuando ha pasado un periodo mínimo sin reforzar la armonía o cuando los controles musicales derivados del EEG cambian de forma suficientemente brusca. Para detectar este último caso, el backend compara el estado actual de actividad, tensión, registro y estabilidad armónica con el estado usado en el último acorde. Si el cambio acumulado supera un umbral, el acorde se interpreta como una señal musical de transición. De este modo, los acordes completos sirven para marcar cambios relevantes del estado EEG sin saturar continuamente la salida.

La Tabla~\ref{tab:impl_note_generator_noteevent} resume la información generada en cada \texttt{NoteEvent}. Esta estructura todavía no es un mensaje MIDI en bytes; representa una nota musical de alto nivel que será programada temporalmente en la etapa siguiente.

\begin{table}[H]
    \centering
    \caption{Información generada por \texttt{NoteGenerator} en cada \texttt{NoteEvent}.}
    \label{tab:impl_note_generator_noteevent}
    \footnotesize
    \setlength{\tabcolsep}{3pt}
    \renewcommand{\arraystretch}{1.10}
    \begin{tabular}{p{0.25\textwidth}p{0.34\textwidth}p{0.32\textwidth}}
        \hline
        \textbf{Campo en \texttt{NoteEvent}} & \textbf{Cómo se obtiene} & \textbf{Papel en la salida musical} \\
        \hline
        \texttt{t\_start} &
        Posición activa dentro del compás. &
        Indica cuándo empieza la nota. \\
        \texttt{t\_end} &
        Siguiente posición activa o final del compás. &
        Indica cuándo debe terminar la nota. \\
        \texttt{pitch\_midi} &
        Nota elegida dentro de la escala, teniendo en cuenta acorde, registro, tensión y continuidad. &
        Define qué nota musical sonará. \\
        \texttt{velocity} &
        Intensidad base, mapa de acentos y \texttt{velocity\_factor}. &
        Define con qué fuerza sonará la nota en MIDI. \\
        \texttt{channel} &
        Canal MIDI configurado en el backend. &
        Indica por qué canal se enviará la nota. \\
        \texttt{program} &
        Programa instrumental configurado. &
        Mantiene la referencia al timbre MIDI usado por la salida. \\
        \hline
    \end{tabular}
\end{table}

Como resultado de esta etapa, el sistema obtiene una lista ordenada de \texttt{NoteEvent}. Cada elemento de la lista representa una nota ya definida a nivel musical: cuándo empieza, cuándo termina, qué nota MIDI debe sonar, con qué \textit{velocity}, por qué canal y con qué programa instrumental. Por tanto, \texttt{NoteGenerator} transforma la plantilla del compás en una secuencia temporal de notas preparada para el scheduler MIDI.

En esa secuencia, las posiciones marcadas del \texttt{Bar} se convierten en instantes de inicio; la distancia hasta la siguiente posición marcada fija la duración; el acorde y la escala limitan las notas posibles; y los controles del \texttt{MusicSegment} ajustan el registro, la tensión, la variedad melódica y la intensidad. Si se activa el acorde completo en el primer slot, pueden generarse varios \texttt{NoteEvent} simultáneos. La conversión final a mensajes \texttt{note\_on} y \texttt{note\_off} no se realiza en esta etapa, sino en el scheduler MIDI descrito en el apartado siguiente.

\subsubsection{Escritura y transmisión MIDI}

La escritura MIDI se divide en dos niveles. El primer nivel es musical y trabaja con \texttt{NoteEvent}; el segundo nivel es MIDI y trabaja con eventos temporizados y bytes. Esta separación permite que \texttt{NoteGenerator} no tenga que conocer el transporte físico, y que \texttt{MidiByteTransport} no tenga que conocer las reglas de generación musical.

\texttt{MidiScheduler}, implementado en \texttt{midi\_live.py}, transforma cada \texttt{NoteEvent} en dos eventos MIDI vivos: un \texttt{note\_on} en el inicio de la nota y un \texttt{note\_off} en su final. Además, el backend programa un \texttt{program\_change} una sola vez al inicio para fijar el programa MIDI de salida. Los eventos se almacenan en una cola temporal ordenada por \texttt{due\_time}, usando el reloj monotónico del sistema.

En cada iteración del backend, \texttt{\_pump\_midi()} llama a \texttt{pop\_due\_events(now, lookahead\_sec=MIDI\_LOOKAHEAD\_SEC)}. El \textit{lookahead} de \SI{0.02}{\second} permite extraer eventos ligeramente por adelantado, de modo que el transporte y el firmware dispongan de margen para emitirlos sin bloquear el ciclo principal. El scheduler no envía nada por sí mismo: solo decide qué eventos están vencidos o próximos a vencer.

La conversión a bytes se realiza mediante \texttt{event\_to\_midi\_bytes()}. Un \texttt{note\_on} se codifica con estado \texttt{0x90 | channel}, nota y \textit{velocity}; un \texttt{note\_off} usa \texttt{0x80 | channel}; un \texttt{control\_change} usa \texttt{0xB0 | channel}; y un \texttt{program\_change} usa \texttt{0xC0 | channel}. Todos los canales se limitan a 0--15 y los datos MIDI a 0--127.

\texttt{MidiByteTransport} recibe los eventos vencidos, los convierte en bytes y llama al \textit{handler} del firmware mediante \texttt{Bridge.call("midi\_bytes", n, b0, b1, b2)}. El campo \texttt{n} indica si el mensaje tiene dos o tres bytes válidos. El firmware no decide qué nota tocar ni interpreta musicalmente el mensaje: recibe los bytes y los escribe por la UART asociada a la salida MIDI física.

La salida física validada usa \texttt{Serial1}/D1 a la velocidad MIDI estándar de \SI{31250}{baudios}, con transmisión invertida en el microcontrolador. Esta configuración pertenece a la capa de firmware y electrónica ya descrita previamente; en 6.3 basta con indicar que la ruta Python--Bridge--firmware desemboca en esa UART física y, desde ella, en el conector MIDI OUT del prototipo.

La Figura~\ref{fig:impl_midi_eventos_bytes} resume la formación de mensajes MIDI desde las notas generadas. Esta figura aclara la diferencia entre nota musical, evento MIDI temporizado, bytes MIDI y transporte físico.

\begin{figure}[H]
    \centering
    \IfFileExists{images/uml/impl_midi_eventos_bytes.png}{%
        \includegraphics[width=0.92\textwidth,height=0.62\textheight,keepaspectratio]{images/uml/impl_midi_eventos_bytes.png}
    }{%
        \IfFileExists{images/uml/impl_midi_eventos_bytes.pdf}{%
            \includegraphics[width=0.92\textwidth,height=0.62\textheight,keepaspectratio]{images/uml/impl_midi_eventos_bytes.pdf}
        }{%
            \fbox{\parbox{0.9\textwidth}{\centering Diagrama pendiente de exportar desde Mermaid: \texttt{images/uml/impl\_midi\_eventos\_bytes.mmd}}}%
        }%
    }
    \caption{Formación de mensajes MIDI en la ruta live. Las notas de alto nivel se transforman en eventos \texttt{note\_on}, \texttt{note\_off} y \texttt{program\_change}; después se convierten a bytes y se envían al firmware mediante \texttt{midi\_bytes}.}
    \label{fig:impl_midi_eventos_bytes}
\end{figure}

Como medida de seguridad, el sistema conserva la ruta \texttt{panic}. Esta acción limpia el scheduler y genera mensajes \texttt{All Sound Off} y \texttt{All Notes Off} para todos los canales. Es necesaria porque una ventana de mala calidad puede bloquear la generación de nuevos compases, pero no elimina automáticamente eventos que ya hubieran sido programados desde una ventana anterior. Por tanto, el \textit{panic} forma parte de la robustez del sistema MIDI y debe mantenerse aunque se simplifique la lógica musical.

La salida MIDI puede desactivarse mediante la variable de entorno \texttt{EEG\_MIDI\_LIVE\_ENABLED}. En ese caso, los eventos pueden seguir generándose y contabilizándose, pero el transporte los marca como descartados en lugar de enviarlos al firmware. Esta opción permite validar generación y scheduler sin forzar siempre el envío físico por la UART MIDI.

\subsection{Implementación de la interfaz web}

% Pendiente de redacción propia.

\subsection{Implementación de la matriz LED}

% Pendiente de redacción propia.

\end{document}

