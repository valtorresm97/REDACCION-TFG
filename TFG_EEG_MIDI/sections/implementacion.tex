% !TeX root = ../main.tex
\documentclass[../main.tex]{subfiles}

\begin{document}

\section{Implementación}

La sección anterior ha descrito el diseño del sistema desde una perspectiva arquitectónica: bloques funcionales, contratos entre módulos y decisiones de diseño adoptadas para transformar una señal EEG real en una salida MIDI física. En esta sección se detalla cómo se ha materializado esa arquitectura en código, indicando qué responsabilidades asume cada parte del sistema y cómo circulan los datos entre ellas.

El criterio seguido en la implementación es mantener separadas las capas de adquisición, transporte, procesamiento, sonificación y salida MIDI. De esta forma, el firmware del microcontrolador se concentra en las tareas de tiempo real asociadas al ADS1299, mientras que el entorno Linux/Python de la Arduino UNO Q recibe los bloques EEG ya escalados, calcula características temporales y espectrales, evalúa la calidad de la señal y genera los eventos musicales. Esta separación evita que la lógica musical dependa directamente de detalles de bajo nivel del convertidor analógico-digital y permite analizar cada etapa de forma independiente.

\subsection{Implementación del firmware de adquisición}

El firmware de adquisición constituye la primera capa ejecutable del sistema. Su papel es transformar la salida digital del ADS1299 en una señal EEG preparada para el backend Python. Para ello configura el convertidor, espera a que haya una nueva conversión disponible, lee el frame por SPI, reconstruye las muestras, las escala a unidades físicas, aplica un filtrado digital básico y agrupa los datos en bloques mediante el evento \texttt{eeg\_block\_uV}.

Dentro del flujo general del sistema, esta capa se encarga de las tareas más cercanas al hardware y al tiempo real. El microcontrolador asegura que las muestras lleguen a Python con una cadencia coherente, en microvoltios y con una estructura de bloque estable. A partir de esos bloques, el backend Python realiza el almacenamiento temporal, el análisis espectral, la evaluación de calidad y la generación de parámetros para la sonificación.

La Figura~\ref{fig:impl_firmware_flujo_adquisicion} resume esta cadena de transformación, desde el frame nativo del ADS1299 hasta el bloque \texttt{eeg\_block\_uV} consumido por el backend Python.

\begin{figure}[H]
    \centering
    \IfFileExists{images/uml/impl_firmware_flujo_adquisicion.pdf}{%
        \includegraphics[width=0.92\textwidth]{images/uml/impl_firmware_flujo_adquisicion.pdf}
    }{%
        \fbox{\parbox{0.9\textwidth}{\centering Diagrama pendiente de exportar desde Mermaid: \texttt{images/uml/impl\_firmware\_flujo\_adquisicion.mmd}}}%
    }
    \caption{Flujo de adquisición implementado en el firmware. El microcontrolador lee frames RDATAC del ADS1299, valida el campo de estado, reconstruye las muestras de 24 bits, las escala, aplica filtrado digital y publica bloques de 8 muestras hacia Linux.}
    \label{fig:impl_firmware_flujo_adquisicion}
\end{figure}

La implementación se reparte en varios archivos. \texttt{sketch.ino} actúa como programa principal: inicializa Bridge, Monitor, ADS1299, filtros y handlers de comunicación, y ejecuta el bucle de adquisición. \texttt{ADS1299\_SafeSPI} encapsula el transporte SPI de bajo nivel; \texttt{ADS1299Plus} implementa la secuencia de arranque, los comandos, el acceso a registros y la lectura de frames; \texttt{ADS1299\_Registers.h} recoge comandos y direcciones con nombres legibles; y \texttt{filters.h} y \texttt{streaming.h} contienen, respectivamente, el acondicionamiento digital y la publicación de bloques hacia Linux.

\subsubsection{Configuración del ADS1299}

El ADS1299-4 es un convertidor analógico-digital de cuatro canales con 24 bits de resolución, orientado a señales EEG y biopotenciales. Integra amplificador de ganancia programable, referencia de tensión, oscilador y filtro digital de decimación. En este proyecto trabaja a \SI{250}{\hertz}, con ganancia 24 y referencia interna de \SI{4.5}{\volt}, de modo que genera una muestra nueva aproximadamente cada \SI{4}{\milli\second}.

La configuración se realiza primero dejando el dispositivo en un estado conocido mediante \texttt{ADS1299Plus::begin()}: se inicializa SPI, se aplica \texttt{RESET}, se detienen conversiones previas, se entra en \texttt{SDATAC} y se lee el registro de identificación. Después, \texttt{configureDefaults()} programa los registros principales, como \texttt{CONFIG1}, \texttt{CONFIG2}, \texttt{CONFIG3}, \texttt{CHnSET}, \texttt{BIAS\_SENSP}, \texttt{BIAS\_SENSN}, \texttt{LOFF\_SENSP} y \texttt{LOFF\_SENSN}. Esta organización evita que el código principal dependa de valores binarios dispersos.

En la versión final del prototipo se utiliza \texttt{ADS\_DIAGNOSTIC\_MODE=5}. Este modo mantiene CH1 como canal EEG principal, con entrada normal y ganancia 24. Los canales CH2--CH4 permanecen dentro del frame para conservar el contrato estructural de cuatro canales, pero se configuran apagados o cortocircuitados y no se interpretan como canales EEG activos finales. La derivación de BIAS se limita a CH1P y CH1N, y la detección \textit{lead-off} se desactiva durante la captura final para no inyectar señales diagnósticas en la señal usada para la sonificación.

\begin{table}[H]
    \centering
    \caption{Configuración final del ADS1299 en el modo de adquisición empleado.}
    \label{tab:impl_ads1299_modo_final}
    \begin{tabular}{p{0.22\textwidth}p{0.33\textwidth}p{0.34\textwidth}}
        \hline
        \textbf{Elemento} & \textbf{Configuración final} & \textbf{Papel en la adquisición} \\
        \hline
        CH1 & Activo, ganancia 24, entrada normal & Canal EEG principal usado por el procesamiento y la sonificación. \\
        CH2--CH4 & Apagados/cortocircuitados en el modo final & Conservados en el payload por contrato, sin interpretarse como EEG activo principal. \\
        BIAS & Derivado de CH1P + CH1N & Referencia de modo común limitada al canal usado. \\
        \textit{Lead-off} & Desactivado & Evita inyección diagnóstica durante la captura final. \\
        Test interno & Desactivado & No se mezcla señal de prueba con la señal real. \\
        Tasa de muestreo & \SI{250}{\hertz} & Frecuencia común entre firmware y backend Python. \\
        \hline
    \end{tabular}
\end{table}

Esta distinción evita una ambigüedad importante: el sistema mantiene un formato de datos de cuatro canales porque el ADS1299-4, el firmware, las capturas y el backend comparten ese contrato. Sin embargo, la validación experimental final se centra en CH1 como canal fisiológico principal.

\subsubsection{Sincronización temporal mediante DRDY y RDATAC}

Una vez configurado el ADS1299, el firmware inicia la adquisición manteniendo activa la señal \texttt{START} y enviando el comando \texttt{RDATAC}. En este modo, el convertidor genera frames de forma continua. La señal \texttt{DRDY}, activa a nivel bajo, indica que una nueva conversión está lista para ser leída.

El firmware no genera muestras por temporización software. Sigue el ritmo impuesto por el ADS1299. Cuando \texttt{DRDY} baja, una interrupción muy breve incrementa el contador \texttt{drdy\_count}. La lectura SPI no se realiza dentro de la interrupción, porque una ISR debe ser corta y no conviene ejecutar operaciones de comunicación, filtrado o publicación en ese contexto. Después, el bucle principal consulta el contador y, si existe una conversión pendiente, llama a \texttt{ADS1299Plus::readFrameRDATAC()}.

Cada frame RDATAC leído en esta configuración ocupa \SI{15}{\byte}: tres bytes de estado y cuatro canales de tres bytes cada uno. El driver baja \texttt{CS}, transfiere quince bytes por SPI enviando \texttt{0x00} como byte de relleno y vuelve a subir \texttt{CS}. El resultado se separa en un campo \texttt{status24} y cuatro muestras de canal.

La Figura~\ref{fig:impl_secuencia_drdy_rdatac} resume esta secuencia. La clave es que \texttt{DRDY} solo marca disponibilidad de dato; la transformación de la muestra se realiza después en el bucle principal.

\begin{figure}[H]
    \centering
    \IfFileExists{images/uml/impl_secuencia_drdy_rdatac.pdf}{%
        \includegraphics[width=0.90\textwidth]{images/uml/impl_secuencia_drdy_rdatac.pdf}
    }{%
        \fbox{\parbox{0.9\textwidth}{\centering Diagrama pendiente de exportar desde Mermaid: \texttt{images/uml/impl\_secuencia\_drdy\_rdatac.mmd}}}%
    }
    \caption{Secuencia simplificada de adquisición. La interrupción de \texttt{DRDY} solo marca la disponibilidad de una muestra; la lectura SPI, el filtrado y la publicación del bloque se realizan posteriormente en el bucle principal.}
    \label{fig:impl_secuencia_drdy_rdatac}
\end{figure}

\subsubsection{Reconstrucción, escalado y filtrado de muestras}

Cada frame RDATAC comienza con un campo de estado de 24 bits. El firmware valida que los bits más significativos coincidan con el prefijo esperado \texttt{0xC00000}. Esta comprobación permite detectar frames incoherentes o problemas de sincronía en la comunicación SPI.

Después del estado aparecen las muestras de canal. Cada canal se transmite como tres bytes en orden \textit{MSB first}. El driver reconstruye esos tres bytes mediante \texttt{unpack24()} y realiza la extensión de signo a 32 bits. Este paso es necesario porque el ADS1299 entrega valores con signo; sin esta extensión, una muestra negativa podría interpretarse como un valor positivo de gran magnitud.

Una vez reconstruido el valor digital de cada canal, el firmware aplica el factor de conversión \texttt{LSB\_V}. En la configuración utilizada, este factor vale \SI{2.235e-8}{\volt} por unidad digital del ADC. Por tanto, la conversión inicial a tensión se expresa como:

\[
    v = c_{\mathrm{ADC}} \cdot \texttt{LSB\_V}
\]

donde \(c_{\mathrm{ADC}}\) es el valor digital con signo reconstruido a partir del frame RDATAC.

La señal resultante se mantiene en voltios para aplicar el acondicionamiento digital: eliminación de continua o paso alto de \SI{0.5}{\hertz}, muesca de \SI{50}{\hertz} y paso bajo de \SI{40}{\hertz}. Tras el filtrado, la muestra se convierte a microvoltios enteros mediante \texttt{volts\_to\_uV\_i32()}, de forma que el backend recibe valores físicos en microvoltios y no valores digitales crudos del ADC.

La Figura~\ref{fig:impl_frame_rdatac_a_bloque_uv} muestra la transformación entre el frame nativo del ADS1299 y el bloque que finalmente recibe Python. La figura ayuda a distinguir dos niveles: un frame RDATAC representa una muestra multicanal; un bloque \texttt{eeg\_block\_uV} agrupa ocho frames ya validados, escalados y filtrados.

\begin{figure}[H]
    \centering
    \IfFileExists{images/uml/impl_frame_rdatac_a_bloque_uv.pdf}{%
        \includegraphics[width=0.80\textwidth]{images/uml/impl_frame_rdatac_a_bloque_uv.pdf}
    }{%
        \fbox{\parbox{0.9\textwidth}{\centering Diagrama pendiente de exportar desde Mermaid: \texttt{images/uml/impl\_frame\_rdatac\_a\_bloque\_uv.mmd}}}%
    }
    \caption{Transformación entre un frame RDATAC del ADS1299 y el bloque \texttt{eeg\_block\_uV}. Cada frame individual se valida, se reconstruye, se escala y se filtra; después se acumulan ocho muestras para formar el bloque enviado a Python.}
    \label{fig:impl_frame_rdatac_a_bloque_uv}
\end{figure}

\subsubsection{Agrupación y envío de bloques hacia Linux}

El firmware no publica una notificación por cada muestra individual. En su lugar, acumula ocho muestras filtradas en la estructura \texttt{EegBlockUV}, definida en \texttt{streaming.h}. Con una frecuencia de muestreo de \SI{250}{\hertz}, cada bloque representa \SI{32}{\milli\second} de señal.

Cada bloque contiene una cabecera con \texttt{block\_idx}, \texttt{first\_sample\_idx} y \texttt{sample\_count}. Después incluye, para cada una de las ocho muestras, el \texttt{status} del ADS1299 y los cuatro canales expresados en microvoltios. Esta estructura define el contrato entre el firmware y Python.

\begin{table}[H]
    \centering
    \caption{Relación temporal entre muestra, bloque y transporte en la adquisición.}
    \label{tab:impl_tiempos_adquisicion}
    \begin{tabular}{p{0.28\textwidth}p{0.25\textwidth}p{0.36\textwidth}}
        \hline
        \textbf{Elemento} & \textbf{Valor} & \textbf{Significado} \\
        \hline
        Frecuencia de muestreo & \SI{250}{\hertz} & Una muestra nueva aproximadamente cada \SI{4}{\milli\second}. \\
        Tamaño de bloque & 8 muestras & Agrupación mínima antes de publicar por Bridge. \\
        Periodo nominal de bloque & \SI{32}{\milli\second} & Intervalo temporal representado por cada \texttt{eeg\_block\_uV}. \\
        Canales por muestra & 4 & Contrato estructural común a firmware, backend y capturas. \\
        \hline
    \end{tabular}
\end{table}

Cuando un bloque se completa, se introduce en una cola circular \texttt{TxBlockRing}. Esta cola desacopla la adquisición de la publicación por Bridge y permite que el firmware siga adquiriendo aunque el envío de un bloque tarde algo más que una iteración del bucle. La publicación se realiza mediante \texttt{Bridge.notify("eeg\_block\_uV", ...)}, enviando la cabecera y las ocho muestras del bloque.

La Figura~\ref{fig:impl_payload_eeg_block_uv} representa la estructura lógica del payload. Cualquier cambio en este formato obligaría a modificar de forma coordinada el firmware, el receptor Python, las capturas CSV y las herramientas de validación.

\begin{figure}[H]
    \centering
    \IfFileExists{images/uml/impl_payload_eeg_block_uv.pdf}{%
        \includegraphics[width=0.86\textwidth]{images/uml/impl_payload_eeg_block_uv.pdf}
    }{%
        \fbox{\parbox{0.9\textwidth}{\centering Diagrama pendiente de exportar desde Mermaid: \texttt{images/uml/impl\_payload\_eeg\_block\_uv.mmd}}}%
    }
    \caption{Estructura lógica del evento \texttt{eeg\_block\_uV}. El firmware transmite una cabecera seguida de ocho muestras; cada muestra conserva el estado del ADS1299 y cuatro canales en microvoltios.}
    \label{fig:impl_payload_eeg_block_uv}
\end{figure}

En el lado Python, el contrato se centraliza en \texttt{eeg\_contract.py}. Este módulo define el nombre del evento, la frecuencia de muestreo, el número de canales, el tamaño de bloque, la máscara de estado del ADS1299 y el número esperado de campos. La función \texttt{parse\_eeg\_block\_values()} valida la longitud del payload y separa estados y muestras. Por su parte, \texttt{EEGReceiver} registra el \textit{callback} \texttt{eeg\_block\_uV}, valida continuidad de índices y encola los bloques para que el backend los entregue después al procesador mediante \texttt{drain\_blocks\_to\_processor()}.

Con esta implementación, la relación entre firmware y backend Python queda definida de forma precisa. El microcontrolador entrega bloques de ocho muestras, con cuatro canales en microvoltios y estado ADS1299 por muestra. Python valida y almacena esos bloques antes de transformarlos en ventanas de análisis. La siguiente parte del capítulo describe cómo el backend consume esta señal ya estructurada para calcular características espectrales y métricas de calidad.



\subsection{Implementación del backend Python}

El backend Python constituye la segunda capa ejecutable de la cadena EEG--MIDI. A diferencia del firmware, esta capa no trabaja con bytes RDATAC ni con cuentas crudas del ADS1299, sino con bloques de señal ya reconstruidos, filtrados y escalados a microvoltios. La frontera entre ambas capas queda definida por el evento \texttt{Bridge.notify("eeg\_block\_uV")}, que entrega al runtime Python bloques de muestras EEG con una estructura fija.

A partir de esa frontera, el backend asume cuatro responsabilidades principales: validar que los bloques recibidos respetan el contrato acordado, conservar una memoria temporal reciente de la señal, extraer características espectrales de la ventana disponible y publicar un estado coherente para los módulos consumidores. Entre estos consumidores se encuentran la interfaz web, las herramientas de captura y validación, y la etapa de sonificación descrita posteriormente. La sonificación no recibe muestras crudas, sino características, diagnósticos y estado de calidad generados por el backend.

La implementación se apoya en cuatro piezas principales. \texttt{EEGReceiver} se encarga de recibir y validar los bloques procedentes del firmware. \texttt{EEGSignalProcessor} mantiene el buffer temporal multicanal y extrae ventanas recientes de señal. \texttt{DSPCore} concentra el cálculo espectral de un canal. Finalmente, \texttt{BackendService} coordina el ciclo de ejecución, drena la cola de recepción, actualiza características y construye el \textit{snapshot} público del sistema. La Figura~\ref{fig:impl_backend_flujo_python} resume esta organización por capas.

\begin{figure}[H]
    \centering
    \IfFileExists{images/uml/impl_backend_flujo_python.pdf}{%
        \includegraphics[width=0.95\textwidth,height=0.36\textheight,keepaspectratio]{images/uml/impl_backend_flujo_python.pdf}
    }{%
        \IfFileExists{images/uml/impl_backend_flujo_python.png}{%
            \includegraphics[width=0.95\textwidth,height=0.36\textheight,keepaspectratio]{images/uml/impl_backend_flujo_python.png}
        }{%
            \fbox{\parbox{0.9\textwidth}{\centering Diagrama pendiente de exportar desde Mermaid: \texttt{images/uml/impl\_backend\_flujo\_python.mmd}}}%
        }%
    }
    \caption{Flujo principal del backend Python organizado por capas. La recepción valida el contrato \texttt{eeg\_block\_uV}, el procesador mantiene la ventana temporal y el bloque DSP/calidad produce las características y métricas publicadas en el \textit{snapshot}.}
    \label{fig:impl_backend_flujo_python}
\end{figure}

\subsubsection{Recepción de muestras EEG}

La recepción en Python continúa el contrato cerrado en el firmware. El evento de entrada se denomina \texttt{eeg\_block\_uV} y está definido por la constante \texttt{EEG\_BLOCK\_EVENT}. Cada evento agrupa \texttt{BLOCK\_SAMPLES=8} muestras para \texttt{NUM\_CH=4} canales. Con una frecuencia de muestreo \texttt{FS\_HZ=250}, cada bloque representa \SI{32}{\milli\second} de señal EEG.

El módulo \texttt{eeg\_contract.py} centraliza la definición del formato compartido entre firmware, backend, capturas y herramientas de análisis. En esta definición se fijan el número de canales, el número de muestras por bloque y las constantes necesarias para validar el estado ADS1299 asociado a cada muestra. La función \texttt{parse\_eeg\_block\_values()} comprueba que el bloque recibido tenga la longitud esperada, que el número de muestras sea coherente y que el contenido pueda separarse correctamente en estados ADS1299 y muestras multicanal.

La función \texttt{EEGReceiver.eeg\_block\_uV()} actúa como \textit{callback} de entrada. En este contexto, un \textit{callback} es una función que se ejecuta automáticamente cuando el runtime recibe un evento desde el firmware. Por este motivo debe mantenerse ligera: su tarea no es calcular la PSD ni generar música, sino validar el bloque, registrar incidencias y encolarlo para su procesamiento posterior.

En concreto, el receptor convierte los argumentos recibidos a enteros, rechaza índices negativos, valida el payload mediante \texttt{parse\_eeg\_block\_values()}, comprueba la continuidad de \texttt{block\_idx} y \texttt{first\_sample\_idx}, verifica el prefijo de estado ADS1299 mediante \texttt{is\_valid\_ads1299\_status()} y actualiza contadores de bloques malformados, pérdidas estimadas, estados inválidos y ocupación de cola. Si el bloque es coherente, se encapsula como \texttt{BlockItem} y se introduce en una cola interna.

La cola desacopla la llegada de datos desde el firmware del procesamiento digital posterior. El \textit{callback} solo valida y encola; el backend vacía la cola en su ciclo normal mediante \texttt{drain\_blocks\_to\_processor()}, entregando bloques completos a \texttt{EEGSignalProcessor.add\_block\_uV()}. Con esta separación, el coste variable del DSP no bloquea la recepción de nuevos eventos.

\subsubsection{Buffer temporal de señal}

\texttt{EEGSignalProcessor} transforma la sucesión de bloques recibidos en una señal temporal continua. Su entrada son bloques en microvoltios con forma lógica \texttt{(n\_samples, num\_channels)}. Antes de escribirlos en memoria, las muestras se convierten a voltios:

\[
    x_{\mathrm{V}}[n,c] =
    x_{\mu\mathrm{V}}[n,c]\cdot 10^{-6}.
\]

Esta conversión mantiene el cálculo interno en unidades del Sistema Internacional, mientras que las métricas de diagnóstico se expresan posteriormente en microvoltios por ser una unidad más interpretable para señales EEG.

Internamente, el procesador emplea un \textit{ring buffer} o buffer circular de tipo \texttt{float32} con forma \texttt{(num\_channels, buffer\_size)}. En un buffer circular, las muestras nuevas se escriben de forma secuencial y, al llegar al final de la memoria reservada, la escritura continúa desde el inicio sobrescribiendo las muestras más antiguas. Esta estrategia evita desplazar matrices completas y permite conservar siempre un historial reciente de señal.

El contrato multicanal se mantiene durante todo el proceso: el buffer conserva cuatro canales aunque la versión final utilice CH1 como canal principal de análisis. En el código, CH1 se selecciona mediante \texttt{channel\_idx=0}, ya que los índices de Python comienzan en cero. El resto de canales se preservan en la estructura de datos, en las capturas y en el contrato de transporte, pero no se presentan como cuatro canales EEG activos de análisis final.

La extracción de características no comienza hasta que existe una ventana temporal suficiente. En este sistema, la ventana de análisis tiene una duración de \SI{4}{\second}. A \SI{250}{\hertz}, esto equivale a 1000 muestras. Una vez llena la ventana, las características no se recalculan muestra a muestra, sino cada \texttt{FEATURE\_HOP\_SAMPLES=64} muestras. Este salto, denominado \textit{hop}, equivale a \SI{256}{\milli\second}. La Tabla~\ref{tab:impl_backend_tiempos} resume las escalas temporales principales.

\[
\begin{array}{rcl}
    T_{\mathrm{bloque}} & = & \dfrac{\mathrm{BLOCK\_SAMPLES}}{\mathrm{FS\_HZ}}
    = \dfrac{8}{250}
    = \SI{0.032}{\second}
    = \SI{32}{\milli\second},\\[2mm]
    N_{\mathrm{ventana}} & = & \mathrm{FEATURE\_WINDOW\_SEC}\cdot\mathrm{FS\_HZ}
    = 4.0\cdot250
    = 1000~\text{muestras},\\[2mm]
    T_{\mathrm{hop}} & = & \dfrac{\mathrm{FEATURE\_HOP\_SAMPLES}}{\mathrm{FS\_HZ}}
    = \dfrac{64}{250}
    = \SI{0.256}{\second}
    = \SI{256}{\milli\second},\\[2mm]
    f_{\mathrm{snapshot}} & = & \dfrac{1}{\mathrm{SNAPSHOT\_PUBLISH\_PERIOD\_SEC}}
    = \dfrac{1}{0.2}
    = \SI{5}{\hertz}.
\end{array}
\]

\begin{table}[H]
    \centering
    \caption{Escalas temporales principales del backend Python.}
    \label{tab:impl_backend_tiempos}
    \renewcommand{\arraystretch}{1.15}
    \begin{tabular}{p{0.30\textwidth}p{0.20\textwidth}p{0.40\textwidth}}
        \hline
        \textbf{Magnitud} & \textbf{Valor} & \textbf{Interpretación} \\
        \hline
        Frecuencia de muestreo & \SI{250}{\hertz} & Ritmo común entre firmware, receptor y procesador. \\
        Tamaño de bloque & 8 muestras & Agrupación mínima recibida por \texttt{eeg\_block\_uV}. \\
        Duración de bloque & \SI{32}{\milli\second} & Tiempo representado por cada bloque recibido. \\
        Ventana de features & \SI{4}{\second} & Tramo reciente de señal usado para el cálculo espectral. \\
        Tamaño de ventana & 1000 muestras & Número de muestras contenidas en una ventana de \SI{4}{\second}. \\
        Hop de features & 64 muestras & Desplazamiento entre cálculos sucesivos. \\
        Duración del hop & \SI{256}{\milli\second} & Periodicidad nominal de actualización de características. \\
        Snapshot & \SI{0.2}{\second} & Publicación lógica del estado a \SI{5}{\hertz}. \\
        \hline
    \end{tabular}
\end{table}

La Figura~\ref{fig:impl_backend_buffer_ventana} resume la relación entre bloque, buffer circular, ventana de análisis y hop de actualización.

\begin{figure}[H]
    \centering
    \IfFileExists{images/uml/impl_backend_buffer_ventana.pdf}{%
        \includegraphics[width=0.95\textwidth,height=0.38\textheight,keepaspectratio]{images/uml/impl_backend_buffer_ventana.pdf}
    }{%
        \IfFileExists{images/uml/impl_backend_buffer_ventana.png}{%
            \includegraphics[width=1\textwidth,height=0.70\textheight,keepaspectratio]{images/uml/impl_backend_buffer_ventana.png}
        }{%
            \fbox{\parbox{0.9\textwidth}{\centering Diagrama pendiente de exportar desde Mermaid: \texttt{images/uml/impl\_backend\_buffer\_ventana.mmd}}}%
        }%
    }
    \caption{Organización temporal del buffer EEG en Python. Los bloques de ocho muestras se convierten a voltios, se escriben en un anillo multicanal y, al completarse la ventana de \SI{4}{\second}, CH1 se analiza cada 64 muestras.}
    \label{fig:impl_backend_buffer_ventana}
\end{figure}

\subsubsection{Extracción de características espectrales}

La base matemática del análisis espectral, la densidad espectral de potencia, el cálculo de potencia por bandas y el método multitaper se ha introducido en el apartado de Conceptos previos. En esta sección se documenta únicamente cómo se integra ese análisis dentro del backend Python.

Cuando la ventana está completa y se cumple el salto de 64 muestras, \texttt{compute\_live\_features()} extrae la ventana reciente de CH1 y delega el cálculo en \texttt{DSPCore}. De esta forma, \texttt{EEGSignalProcessor} se encarga de la memoria temporal y de la selección de ventana, mientras que \texttt{DSPCore} se limita al análisis espectral de una señal unidimensional.

En la ruta en vivo se utiliza \texttt{psd\_method="multitaper"}. Antes de estimar la PSD, \texttt{DSPCore} aplica un preprocesado robusto sobre la ventana seleccionada. Después calcula la PSD, integra potencia por bandas y resume la ventana mediante un conjunto compacto de características. En la ruta de tiempo real no se publica el espectro completo, sino un diccionario de \textit{features} preparado para ser consumido por el resto del sistema. La Tabla~\ref{tab:impl_backend_features} resume las claves principales.

\begin{table}[H]
    \centering
    \caption{Diccionario principal de características espectrales generado por el backend.}
    \label{tab:impl_backend_features}
    \renewcommand{\arraystretch}{1.15}
    \begin{tabular}{p{0.24\textwidth}p{0.25\textwidth}p{0.41\textwidth}}
        \hline
        \textbf{Grupo} & \textbf{Clave} & \textbf{Significado} \\
        \hline
        Amplitud global & \texttt{rms} & Valor eficaz de la ventana preprocesada. Resume la energía temporal de la señal analizada. \\
        Pico espectral global & \texttt{peak\_freq} & Frecuencia de máxima potencia dentro de la PSD estimada. \\
        Picos por banda & \texttt{peak\_delta}, \texttt{peak\_theta}, \texttt{peak\_alpha}, \texttt{peak\_beta}, \texttt{peak\_gamma} & Frecuencia dominante dentro de cada banda, cuando existe contenido suficiente en el rango correspondiente. \\
        Potencia absoluta & \texttt{bandpower\_abs} & Diccionario con la potencia integrada en delta, theta, alpha, beta y gamma. \\
        Potencia relativa & \texttt{bandpower\_rel} & Diccionario normalizado que expresa la contribución relativa de cada banda respecto a la potencia total considerada. \\
        Banda dominante & \texttt{dominant\_band} & Banda con mayor potencia relativa en la ventana reciente. Se calcula al construir el \textit{snapshot}. \\
        Relación alpha/beta & \texttt{alpha\_beta\_ratio} & Cociente entre potencia relativa alpha y beta. Se publica como descriptor complementario, no como indicador clínico. \\
        \hline
    \end{tabular}
\end{table}

Las bandas empleadas por el backend son delta, theta, alpha, beta y gamma, con rangos coherentes con los descritos en Conceptos previos. En esta implementación se usan como descriptores cuantitativos de la ventana reciente y como entrada algorítmica para módulos posteriores. No se emplean para realizar diagnóstico clínico ni para inferir estados médicos del usuario.

Además de las características espectrales, el procesador calcula un segundo bloque de información denominado \texttt{diagnostics}. Este bloque no describe el contenido musical ni espectral de la señal, sino su estado observacional: amplitud en microvoltios, offset, dispersión, posibles saturaciones, señal plana, saltos bruscos y presencia relativa de energía en torno a \SI{50}{\hertz}. La Tabla~\ref{tab:impl_backend_diagnostics_quality} separa las tres salidas principales de esta etapa.

\begin{table}[H]
    \centering
    \caption{Diferencia entre características, diagnósticos y evaluación de calidad.}
    \label{tab:impl_backend_diagnostics_quality}
    \renewcommand{\arraystretch}{1.15}
    \begin{tabular}{p{0.22\textwidth}p{0.36\textwidth}p{0.32\textwidth}}
        \hline
        \textbf{Bloque} & \textbf{Contenido principal} & \textbf{Finalidad} \\
        \hline
        \texttt{features} & \texttt{rms}, picos espectrales, potencias absolutas y relativas por banda, banda dominante y relación alpha/beta & Describir la ventana EEG reciente mediante variables compactas. \\
        \texttt{diagnostics} & \texttt{rms\_uv}, \texttt{mean\_uv}, \texttt{std\_uv}, \texttt{ptp\_uv}, percentiles, \texttt{saturation\_fraction}, \texttt{flatline}, \texttt{abrupt\_jumps}, \texttt{line\_50\_ratio} y avisos & Detectar problemas observacionales de señal sin modificar el pipeline DSP. \\
        \texttt{spectral\_quality} & \texttt{score}, \texttt{state}, \texttt{gate\_factor}, \texttt{valid\_for\_sonification}, \texttt{freeze\_recommended}, \texttt{warnings} y penalizaciones & Decidir si la ventana puede alimentar los módulos posteriores con normalidad, atenuación o congelación. \\
        \hline
    \end{tabular}
\end{table}

El módulo \texttt{spectral\_quality.py} combina \texttt{features}, \texttt{diagnostics}, métricas de recepción y disponibilidad de ventana. A partir de esa información asigna una puntuación de calidad, un estado y un factor de puerta. Este bloque no recalcula la PSD ni altera la señal EEG; actúa como una capa de protección del runtime para evitar que ventanas con artefactos evidentes alimenten sin control a la sonificación.

La Figura~\ref{fig:impl_backend_features_quality} resume esta separación entre cálculo espectral, diagnóstico observacional y evaluación de calidad.

\begin{figure}[H]
    \centering
    \IfFileExists{images/uml/impl_backend_features_quality.pdf}{%
        \includegraphics[width=0.95\textwidth,height=0.34\textheight,keepaspectratio]{images/uml/impl_backend_features_quality.pdf}
    }{%
        \IfFileExists{images/uml/impl_backend_features_quality.png}{%
            \includegraphics[width=0.95\textwidth,height=0.34\textheight,keepaspectratio]{images/uml/impl_backend_features_quality.png}
        }{%
            \fbox{\parbox{0.9\textwidth}{\centering Diagrama pendiente de exportar desde Mermaid: \texttt{images/uml/impl\_backend\_features\_quality.mmd}}}%
        }%
    }
    \caption{Separación entre extracción espectral, diagnóstico observacional y evaluación de calidad en el backend Python. La ventana CH1 alimenta \texttt{DSPCore}; las características y diagnósticos se combinan después en \texttt{spectral\_quality} antes de publicarse en el \textit{snapshot}.}
    \label{fig:impl_backend_features_quality}
\end{figure}

\subsubsection{Publicación de estado y métricas}

\texttt{BackendService} coordina el ciclo de ejecución del backend. En cada iteración drena bloques desde \texttt{EEGReceiver}, actualiza el buffer temporal, comprueba si corresponde calcular nuevas características, obtiene diagnósticos, evalúa \texttt{spectral\_quality} y publica periódicamente un \textit{snapshot}. El \textit{snapshot} es una vista estructurada del estado actual del sistema, consumida por la interfaz web, las herramientas de captura, los registros en disco y los módulos posteriores.

El estado principal del runtime se publica dentro de \texttt{status.state}. Este estado no indica la calidad de la señal, sino la fase de ejecución en la que se encuentra el backend. La Figura~\ref{fig:impl_backend_runtime_states} muestra, en paralelo, la diferencia entre los estados de ejecución y los estados de calidad espectral.

\begin{figure}[H]
    \centering
    \IfFileExists{images/uml/impl_backend_runtime_states.pdf}{%
        \includegraphics[width=0.90\textwidth,height=0.40\textheight,keepaspectratio]{images/uml/impl_backend_runtime_states.pdf}
    }{%
        \IfFileExists{images/uml/impl_backend_runtime_states.png}{%
            \includegraphics[width=0.90\textwidth,height=0.40\textheight,keepaspectratio]{images/uml/impl_backend_runtime_states.png}
        }{%
            \fbox{\parbox{0.9\textwidth}{\centering Diagrama pendiente de exportar desde Mermaid: \texttt{images/uml/impl\_backend\_runtime\_states.mmd}}}%
        }%
    }
    \caption{Separación entre estado de ejecución y estado de calidad en el backend Python. \texttt{status.state} describe la fase del runtime, mientras que \texttt{spectral\_quality.state} resume la fiabilidad de la última ventana analizada.}
    \label{fig:impl_backend_runtime_states}
\end{figure}

La diferencia entre estado del runtime y estado de calidad es importante. \texttt{status.state} indica si el sistema está esperando datos, llenando la ventana o con características disponibles. En cambio, \texttt{spectral\_quality.state} indica si la última ventana EEG parece limpia, utilizable con precaución, sospechosa de artefacto o no recomendable para sonificación.

\begin{table}[H]
    \centering
    \caption{Estados principales publicados por el backend.}
    \label{tab:impl_backend_estados}
    \renewcommand{\arraystretch}{1.15}
    \begin{tabular}{p{0.25\textwidth}p{0.25\textwidth}p{0.40\textwidth}}
        \hline
        \textbf{Bloque} & \textbf{Estado} & \textbf{Significado} \\
        \hline
        \texttt{status} & \texttt{waiting\_for\_data} & Todavía no se han recibido bloques EEG desde el firmware. \\
        \texttt{status} & \texttt{waiting\_for\_window} & Hay recepción, pero aún no se han acumulado \SI{4}{\second} de señal. \\
        \texttt{status} & \texttt{receiving} & La ventana está disponible, pero todavía no existen features recientes. \\
        \texttt{status} & \texttt{features\_ready} & Existe una extracción espectral disponible para los módulos consumidores. \\
        \texttt{spectral\_quality} & \texttt{clean} & Ventana considerada adecuada para uso normal. \\
        \texttt{spectral\_quality} & \texttt{usable\_with\_caution} & Ventana utilizable, pero con alguna penalización leve. \\
        \texttt{spectral\_quality} & \texttt{artifact\_suspected} & Ventana sospechosa; se recomienda atenuar su efecto. \\
        \texttt{spectral\_quality} & \texttt{bad} & Ventana no recomendable; puede activarse congelación o bloqueo de la sonificación. \\
        \hline
    \end{tabular}
\end{table}

Antes de detallar los grupos publicados, conviene señalar que el \textit{snapshot} no introduce una etapa nueva de procesado, sino que actúa como una vista agrupada del estado del backend. En él se reúnen la configuración activa del sistema, el estado de ejecución, las métricas de recepción, las características espectrales calculadas, los diagnósticos observacionales, la evaluación de calidad y cierta información auxiliar de captura, rendimiento y errores. De esta forma, los módulos consumidores no acceden directamente al receptor, al buffer o al núcleo DSP, sino a una representación coherente y actualizada del estado global del backend.

La Figura~\ref{fig:impl_backend_snapshot_schema} resume esta organización del \textit{snapshot} como punto de salida común del backend Python.

\begin{figure}[H]
    \centering
    \IfFileExists{images/uml/impl_backend_snapshot_schema.pdf}{%
        \includegraphics[width=0.95\textwidth,height=0.42\textheight,keepaspectratio]{images/uml/impl_backend_snapshot_schema.pdf}
    }{%
        \IfFileExists{images/uml/impl_backend_snapshot_schema.png}{%
            \includegraphics[width=0.95\textwidth,height=0.42\textheight,keepaspectratio]{images/uml/impl_backend_snapshot_schema.png}
        }{%
            \fbox{\parbox{0.9\textwidth}{\centering Diagrama pendiente de exportar desde Mermaid: \texttt{images/uml/impl\_backend\_snapshot\_schema.mmd}}}%
        }%
    }
    \caption{Estructura del \textit{snapshot} publicado por el backend. \texttt{BackendService.\_build\_snapshot()} agrupa configuración, estado, recepción, características, diagnósticos, calidad, captura, rendimiento y errores en una vista común para WebUI, sonificación y validación.}
    \label{fig:impl_backend_snapshot_schema}
\end{figure}

El \textit{snapshot} se organiza como un diccionario con varios grupos. La Tabla~\ref{tab:impl_backend_snapshot} resume los más relevantes para la implementación del backend.

\begin{table}[H]
    \centering
    \caption{Grupos principales del \textit{snapshot} publicado por el backend Python.}
    \label{tab:impl_backend_snapshot}
    \renewcommand{\arraystretch}{1.15}
    \begin{tabular}{p{0.22\textwidth}p{0.36\textwidth}p{0.32\textwidth}}
        \hline
        \textbf{Grupo} & \textbf{Contenido principal} & \textbf{Finalidad} \\
        \hline
        \texttt{config} & \texttt{fs\_hz}, \texttt{num\_ch}, \texttt{feature\_window\_sec}, \texttt{feature\_hop\_samples}, \texttt{psd\_method}, \texttt{channel\_idx} & Registrar la configuración activa del backend. \\
        \texttt{status} & Estado general, disponibilidad de ventana y último índice de muestra recibido & Indicar si el sistema espera datos, llena buffer o dispone de features. \\
        \texttt{rx} & Tasas de recepción, totales de frames y bloques, pérdidas, cola, bloques malformados y estados inválidos & Vigilar la estabilidad del contrato MCU--Python. \\
        \texttt{features} & RMS, picos, potencias por banda, banda dominante y relación alpha/beta & Exponer el resumen espectral de la ventana reciente. \\
        \texttt{diagnostics} & Métricas de amplitud en \si{\micro\volt}, percentiles, saturación, señal plana, saltos, ratio de \SI{50}{\hertz} y avisos & Evaluar observacionalmente la calidad de la ventana. \\
        \texttt{spectral\_quality} & Puntuación, estado, factor de puerta, validez para sonificación, recomendación de congelación y penalizaciones & Regular el uso de las features por los módulos posteriores. \\
        \texttt{capture} & Estado de captura y metadatos asociados & Permitir adquisición y registro de sesiones reales. \\
        \texttt{performance} & Periodos de publicación y cadencias principales del runtime & Documentar los ritmos de actualización del sistema. \\
        \texttt{errors} & Contadores de errores acumulados en generación musical o bombeo MIDI & Facilitar depuración y validación. \\
        \hline
    \end{tabular}
\end{table}

Las métricas de recepción proceden de \texttt{EEGReceiver.get\_window\_metrics()}. Incluyen tasas de frames y bloques, totales recibidos, pérdidas estimadas, bloques malformados, estados ADS1299 inválidos, ocupación de cola y descartes. Estas métricas permiten detectar si el backend recibe datos de forma estable o si existe acumulación entre la llegada por Bridge y el consumo por el procesador.

El \textit{snapshot} se publica con \texttt{SNAPSHOT\_PUBLISH\_PERIOD\_SEC=0.2}, aproximadamente a \SI{5}{\hertz}. Esta frecuencia es suficiente para monitorización, visualización y validación sin convertir la publicación de estado en la tarea dominante del backend.

Con esta organización, el backend Python queda delimitado como una capa de recepción, memoria temporal, extracción espectral, diagnóstico observacional y publicación de estado. La generación musical, la interfaz web y la matriz LED se apoyan en las estructuras publicadas por este backend, pero no alteran el contrato \texttt{eeg\_block\_uV}, la frecuencia de muestreo ni el contenido del buffer EEG.


\subsection{Implementación de la sonificación}

La sonificación constituye la tercera capa ejecutable de la cadena EEG--MIDI. Su entrada no son muestras crudas ni ventanas temporales de señal, sino el resultado ya producido por el backend descrito en la sección anterior: características espectrales, diagnósticos observacionales y estado de calidad de la ventana. A partir de esta representación intermedia, el sistema construye controles musicales normalizados, genera una estructura musical jerárquica y finalmente programa mensajes MIDI para la salida física.

La ruta implementada evita una traducción directa de una banda EEG a una nota concreta. En su lugar, el sistema separa tres tipos de información: las características derivadas del EEG, la evaluación de calidad de la ventana y la configuración musical elegida por el usuario o fijada por el backend. Con esta separación, el EEG modula variables como actividad, densidad, registro, estabilidad, dinámica y probabilidad de nota, mientras que elementos como la escala, la nota raíz, la nota principal, el canal MIDI y el programa se mantienen como parámetros musicales de configuración.

La Figura~\ref{fig:impl_sonificacion_flujo} resume el flujo principal implementado. La figura debe leerse como continuación directa de la sección 6.2: el bloque DSP y el \textit{quality gate} ya han producido sus salidas, y la sonificación comienza en \texttt{SonificationFeatureAdapter}.

\begin{figure}[H]
    \centering
    \IfFileExists{images/uml/impl_sonificacion_flujo.png}{%
        \includegraphics[width=0.98\textwidth,height=0.42\textheight,keepaspectratio]{images/uml/impl_sonificacion_flujo.png}
    }{%
        \IfFileExists{images/uml/impl_sonificacion_flujo.pdf}{%
            \includegraphics[width=0.98\textwidth,height=0.42\textheight,keepaspectratio]{images/uml/impl_sonificacion_flujo.pdf}
        }{%
            \fbox{\parbox{0.9\textwidth}{\centering Diagrama pendiente de exportar desde Mermaid: \texttt{images/uml/impl\_sonificacion\_flujo.mmd}}}%
        }%
    }
    \caption{Flujo principal de sonificación implementado. Las características EEG y la calidad de señal se transforman en controles musicales, después en segmentos, compases, notas y finalmente en eventos MIDI enviados al firmware mediante \texttt{midi\_bytes}.}
    \label{fig:impl_sonificacion_flujo}
\end{figure}

\subsubsection{Segmentación EEG}

En esta sección, el término segmentación no se refiere a una segmentación \textit{offline} de registros EEG ni a la detección clínica de estados cerebrales. La implementación trabaja sobre la ventana viva ya calculada por el backend: CH1 se analiza en ventanas de \SI{4}{\second}, actualizadas cada 64 muestras, y el resultado de esa ventana se entrega a la sonificación como un diccionario de características. Por tanto, el primer paso de 6.3 consiste en transformar una ventana EEG ya resumida en controles musicales, no en volver a procesar la señal temporal.

El módulo \texttt{sonification\_features.py} concentra esta frontera. Su documentación interna establece explícitamente que no calcula DSP, no importa \texttt{EEGSignalProcessor} y no depende de \texttt{BackendService}; recibe el diccionario ya calculado por \texttt{compute\_live\_features()} y lo convierte en una estructura \texttt{SonificationFeatures}. Esta decisión mantiene la separación de responsabilidades: el cálculo espectral pertenece a 6.2, mientras que 6.3 empieza con una representación musicalmente utilizable de esas características.

La clase \texttt{SonificationFeatures} agrupa controles normalizados en el intervalo \([0,1]\). Estos controles son reportables en el \textit{snapshot}, en la WebUI, en capturas y en la memoria del TFG. Los principales son \texttt{alpha\_drive}, \texttt{beta\_gamma\_drive}, \texttt{rms\_beta\_activity}, \texttt{band\_driven\_density}, \texttt{spectral\_register}, \texttt{alpha\_stability}, \texttt{rms\_band\_velocity} y \texttt{band\_note\_probability}. El objeto conserva además información trazable, como RMS en voltios y microvoltios, frecuencia pico, banda dominante y potencias por banda.

La Tabla~\ref{tab:impl_sonificacion_controles} resume el significado de estos controles dentro del motor musical. Esta tabla no debe interpretarse como una tabla clínica de bandas EEG, sino como una descripción del mapeo algorítmico utilizado para generar música.

\begin{table}[H]
    \centering
    \caption{Controles principales usados por la sonificación.}
    \label{tab:impl_sonificacion_controles}
    \renewcommand{\arraystretch}{1.15}
    \begin{tabular}{p{0.23\textwidth}p{0.25\textwidth}p{0.28\textwidth}p{0.14\textwidth}}
        \hline
        \textbf{Control} & \textbf{Origen EEG} & \textbf{Interpretación musical} & \textbf{Rango/efecto} \\
        \hline
        \texttt{alpha\_drive} & Relación relativa entre alfa y beta & Tendencia a estabilidad o reposo musical & 0--1 \\
        \texttt{beta\_gamma\_drive} & Peso de beta y gamma & Tensión, síncopa o actividad rápida & 0--1 \\
        \texttt{rms\_beta\_activity} & RMS normalizado combinado con beta/gamma & Actividad global del compás & 0--1 \\
        \texttt{band\_driven\_density} & Densidad derivada de bandas rápidas y RMS & Cantidad de eventos rítmicos & 0--1 \\
        \texttt{spectral\_register} & Frecuencia pico y peso de bandas rápidas & Desplazamiento hacia registro grave/agudo & 0--1 \\
        \texttt{alpha\_stability} & Alfa, theta y nivel RMS & Estabilidad armónica relativa & 0--1 \\
        \texttt{rms\_band\_velocity} & RMS y bandas rápidas & Dinámica o \textit{velocity} MIDI & 0--1 \\
        \texttt{band\_note\_probability} & Densidad espectral musicalizada & Probabilidad de activar notas & 0--1 \\
        \texttt{quality\_gate} & Estado de calidad espectral & Atenuación o bloqueo musical & 0--1 \\
        \hline
    \end{tabular}
\end{table}

El \texttt{quality\_gate} se incorpora a la sonificación antes de generar nuevo material musical. Si la calidad de la ventana es alta, los controles se aplican con normalidad; si la ventana presenta artefactos o baja fiabilidad, los controles sensibles se atenúan. En particular, la implementación reduce la actividad, la densidad, la velocidad y la probabilidad de nota cuando el factor de puerta disminuye. Si no existen características válidas o el \textit{score} cae por debajo del umbral de uso, la sonificación deja de generar nuevo material útil hasta recibir una ventana posterior válida.

La Figura~\ref{fig:impl_sonificacion_quality_gate} muestra esta relación entre características, diagnósticos, calidad y controles musicales. La finalidad de la figura es aclarar que la calidad no modifica la adquisición ni recalcula el DSP, sino que actúa como una puerta entre las características EEG y el motor musical.

\begin{figure}[H]
    \centering
    \IfFileExists{images/uml/impl_sonificacion_quality_gate.png}{%
        \includegraphics[width=0.95\textwidth,height=0.42\textheight,keepaspectratio]{images/uml/impl_sonificacion_quality_gate.png}
    }{%
        \IfFileExists{images/uml/impl_sonificacion_quality_gate.pdf}{%
            \includegraphics[width=0.95\textwidth,height=0.42\textheight,keepaspectratio]{images/uml/impl_sonificacion_quality_gate.pdf}
        }{%
            \fbox{\parbox{0.9\textwidth}{\centering Diagrama pendiente de exportar desde Mermaid: \texttt{images/uml/impl\_sonificacion\_quality\_gate.mmd}}}%
        }%
    }
    \caption{Papel del \textit{quality gate} en la sonificación. La calidad de la ventana decide si los controles se aplican plenamente, se atenúan o se evita generar nuevo material musical a partir de una ventana no fiable.}
    \label{fig:impl_sonificacion_quality_gate}
\end{figure}

Además de aplicar la puerta de calidad, \texttt{SonificationFeatureAdapter} introduce memoria temporal. El RMS se normaliza respecto a una línea base adaptativa lenta y varios controles se suavizan mediante una media exponencial. Esta decisión reduce cambios bruscos entre ventanas consecutivas y evita que pequeñas fluctuaciones espectrales se traduzcan en saltos musicales demasiado abruptos. El suavizado no oculta que una ventana sea mala: los metadatos de calidad se conservan y el estado \texttt{valid} sigue condicionando la generación posterior.

\subsubsection{Construcción de segmentos musicales}

Una vez obtenidos los controles normalizados, el backend construye un estado musical intermedio mediante \texttt{MusicSegmentBuilder}. Este módulo tampoco calcula DSP ni lee muestras EEG. Su entrada principal es el objeto \texttt{SonificationFeatures} ya suavizado, junto con la escala musical y la nota principal definidas por configuración. La salida es un \texttt{MusicSegment}, que resume la información necesaria para generar un compás en tiempo real.

El segmento musical no debe entenderse como un fragmento EEG clínico, sino como una ventana musical viva. Contiene un intervalo temporal, la escala activa, la nota principal, una cadencia rítmica discreta y varios controles musicales derivados del EEG. La duración se corresponde en la ruta live con \texttt{MUSIC\_BAR\_SEC}, cuyo valor por defecto es \SI{2}{\second}. Así, cada generación musical produce normalmente un compás asociado al estado EEG reciente.

\texttt{MusicSegmentBuilder} convierte la densidad continua \texttt{band\_driven\_density} en una cadencia discreta \texttt{LOW}, \texttt{MEDIUM} o \texttt{HIGH}. Para evitar que el ritmo cambie constantemente por fluctuaciones pequeñas, el builder utiliza histéresis. Esta decisión mejora la continuidad perceptiva: una variación leve entre dos ventanas consecutivas no obliga necesariamente a cambiar el patrón rítmico del siguiente compás.

El segmento conserva claramente la separación entre EEG y configuración musical. La nota raíz, la escala y la nota principal no se infieren directamente de la señal: proceden de la configuración fija inicial o de la WebUI. El EEG aporta el grado de actividad, tensión, estabilidad, registro, velocidad y probabilidad de nota. La Figura~\ref{fig:impl_sonificacion_controles} representa esta separación para evitar interpretar la sonificación como una traducción literal de EEG a melodía.

\begin{figure}[H]
    \centering
    \IfFileExists{images/uml/impl_sonificacion_controles.png}{%
        \includegraphics[width=0.95\textwidth,height=0.46\textheight,keepaspectratio]{images/uml/impl_sonificacion_controles.png}
    }{%
        \IfFileExists{images/uml/impl_sonificacion_controles.pdf}{%
            \includegraphics[width=0.95\textwidth,height=0.46\textheight,keepaspectratio]{images/uml/impl_sonificacion_controles.pdf}
        }{%
            \fbox{\parbox{0.9\textwidth}{\centering Diagrama pendiente de exportar desde Mermaid: \texttt{images/uml/impl\_sonificacion\_controles.mmd}}}%
        }%
    }
    \caption{Separación entre características EEG, calidad de señal y configuración musical. El EEG modula controles continuos; la nota raíz, la nota principal, la escala, el canal, el programa y la duración del compás proceden de la configuración musical.}
    \label{fig:impl_sonificacion_controles}
\end{figure}

La construcción de segmentos también permite bloquear la generación si las características no son válidas. En el backend, \texttt{\_maybe\_generate\_music()} no continúa si no existe \texttt{\_last\_sonification} o si el objeto no está marcado como válido. De esta forma, una ventana mala no produce un nuevo compás, aunque puedan quedar eventos MIDI ya programados desde una ventana anterior.

\subsubsection{Generación de compases}

El siguiente nivel de la jerarquía es el compás. \texttt{BarGenerator} recibe un \texttt{MusicSegment} y produce un objeto \texttt{Bar}. Este objeto contiene el índice del compás, tiempos de inicio y fin, la raíz del acorde, las notas del acorde, un patrón de posiciones activas y una envolvente musical sintética. El compás funciona como puente entre controles continuos y notas individuales.

La implementación usa una cuadrícula de 16 posiciones por compás: cuatro pulsos con cuatro subdivisiones por pulso. Cada posición puede estar activa o en silencio. El primer slot se fuerza como ancla musical para que el compás tenga un punto de referencia estable. El número objetivo de notas depende de la cadencia rítmica \texttt{LOW}, \texttt{MEDIUM} o \texttt{HIGH}, y se ajusta ligeramente con la actividad estimada.

Los pesos de selección de slots combinan reglas musicales y controles EEG. La base favorece el primer pulso, los tiempos principales y ciertas subdivisiones. Después, la actividad incrementa la probabilidad global de eventos, la tensión favorece síncopas u \textit{offbeats}, y \texttt{band\_note\_probability} ajusta la probabilidad general de activar posiciones. Por tanto, el EEG no decide manualmente cada slot, sino que modifica una distribución musical ponderada.

En paralelo, el compás selecciona un acorde diatónico a partir de la escala activa. La estabilidad armónica y la tensión determinan si se tiende a un grado estable, intermedio o de mayor tensión. La implementación utiliza histéresis armónica para evitar saltos continuos entre grados por cambios pequeños de la señal. Esto mantiene una salida más coherente en tiempo real.

La Figura~\ref{fig:impl_sonificacion_segmento_compas_nota} resume la relación entre segmento, compás y notas. La misma figura se utiliza también como guía para la subsección siguiente, porque muestra que las notas se generan después de disponer de escala, acorde, patrón rítmico y controles musicales.

\begin{figure}[H]
    \centering
    \IfFileExists{images/uml/impl_sonificacion_segmento_compas_nota.png}{%
        \includegraphics[width=0.98\textwidth,height=0.46\textheight,keepaspectratio]{images/uml/impl_sonificacion_segmento_compas_nota.png}
    }{%
        \IfFileExists{images/uml/impl_sonificacion_segmento_compas_nota.pdf}{%
            \includegraphics[width=0.98\textwidth,height=0.46\textheight,keepaspectratio]{images/uml/impl_sonificacion_segmento_compas_nota.pdf}
        }{%
            \fbox{\parbox{0.9\textwidth}{\centering Diagrama pendiente de exportar desde Mermaid: \texttt{images/uml/impl\_sonificacion\_segmento\_compas\_nota.mmd}}}%
        }%
    }
    \caption{Jerarquía musical empleada por la sonificación. Los controles EEG suavizados y la configuración musical construyen un segmento; el segmento genera un compás con acorde y patrón rítmico; el generador de notas produce eventos \texttt{NoteEvent}.}
    \label{fig:impl_sonificacion_segmento_compas_nota}
\end{figure}

\subsubsection{Generación de notas}

\texttt{NoteGenerator} convierte el \texttt{MusicSegment} y el \texttt{Bar} en una lista de \texttt{NoteEvent}. Esta estructura representa una nota musical de alto nivel, pero todavía no es un mensaje MIDI. Cada \texttt{NoteEvent} contiene tiempo de inicio, tiempo de fin, altura MIDI, \textit{velocity}, canal y programa. La conversión a \texttt{note\_on} y \texttt{note\_off} se realiza después, en el scheduler MIDI.

La selección de alturas se realiza dentro de la escala configurada. Primero se calcula un centro de registro alrededor de la nota principal. El control \texttt{spectral\_register} desplaza suavemente ese centro hacia registros más graves o más agudos, pero la nota principal sigue actuando como referencia musical. A partir de ese centro se construye un conjunto de notas de la escala dentro de un radio determinado, y se separan notas de acorde y notas de paso.

Las posiciones fuertes del compás tienden a utilizar notas del acorde, mientras que las posiciones intermedias pueden usar notas de paso cuando existen candidatos adecuados. La tensión EEG desplaza el objetivo melódico y puede permitir intervalos algo mayores, mientras que la actividad y la probabilidad influyen en la variedad. Aun así, el generador limita los saltos respecto a la nota anterior para evitar melodías erráticas y mantiene las alturas dentro del rango MIDI permitido.

La dinámica se calcula mediante la función de \textit{velocity}. Esta combina un acento musical base según la posición rítmica, el factor EEG \texttt{rms\_band\_velocity} y la envolvente sintética del compás. El resultado se acota entre una velocidad mínima y máxima para evitar notas demasiado débiles o excesivamente agresivas. En este punto, el EEG afecta a la intensidad y al carácter de la nota, pero no se interpreta como una medida clínica de activación cerebral.

La duración de cada nota se obtiene a partir de la cuadrícula del compás. Una nota dura hasta el siguiente slot activo o hasta el final del compás. Si por seguridad se detectara una duración nula o negativa, se impone una duración mínima. Esta regla es importante para el flujo MIDI posterior, porque cada nota debe tener un inicio y un final definidos para evitar notas colgadas.

En el primer slot, el sistema puede generar el acorde completo como marcador armónico. El backend decide si se reproduce ese acorde completo según un periodo mínimo y según si los controles musicales han cambiado de forma suficiente. Así se evita que cada compás repita un acorde completo de forma innecesaria, manteniendo una textura más ligera cuando el estado EEG no cambia significativamente.

\subsubsection{Escritura y transmisión MIDI}

La escritura MIDI se divide en dos niveles. El primer nivel es musical y trabaja con \texttt{NoteEvent}; el segundo nivel es MIDI y trabaja con eventos temporizados y bytes. Esta separación permite que \texttt{NoteGenerator} no tenga que conocer el transporte físico, y que \texttt{MidiByteTransport} no tenga que conocer las reglas de generación musical.

\texttt{MidiScheduler}, implementado en \texttt{midi\_live.py}, transforma cada \texttt{NoteEvent} en dos eventos MIDI vivos: un \texttt{note\_on} en el inicio de la nota y un \texttt{note\_off} en su final. Además, el backend programa un \texttt{program\_change} una sola vez al inicio para fijar el programa MIDI de salida. Los eventos se almacenan en una cola temporal ordenada por \texttt{due\_time}, usando el reloj monotónico del sistema.

En cada iteración del backend, \texttt{\_pump\_midi()} llama a \texttt{pop\_due\_events(now, lookahead\_sec=MIDI\_LOOKAHEAD\_SEC)}. El \textit{lookahead} de \SI{0.02}{\second} permite extraer eventos ligeramente por adelantado, de modo que el transporte y el firmware dispongan de margen para emitirlos sin bloquear el ciclo principal. El scheduler no envía nada por sí mismo: solo decide qué eventos están vencidos o próximos a vencer.

La conversión a bytes se realiza mediante \texttt{event\_to\_midi\_bytes()}. Un \texttt{note\_on} se codifica con estado \texttt{0x90 | channel}, nota y \textit{velocity}; un \texttt{note\_off} usa \texttt{0x80 | channel}; un \texttt{control\_change} usa \texttt{0xB0 | channel}; y un \texttt{program\_change} usa \texttt{0xC0 | channel}. Todos los canales se limitan a 0--15 y los datos MIDI a 0--127.

\texttt{MidiByteTransport} recibe los eventos vencidos, los convierte en bytes y llama al \textit{handler} del firmware mediante \texttt{Bridge.call("midi\_bytes", n, b0, b1, b2)}. El campo \texttt{n} indica si el mensaje tiene dos o tres bytes válidos. El firmware no decide qué nota tocar ni interpreta musicalmente el mensaje: recibe los bytes y los escribe por la UART asociada a la salida MIDI física.

La salida física validada usa \texttt{Serial1}/D1 a la velocidad MIDI estándar de \SI{31250}{baudios}, con transmisión invertida en el microcontrolador. Esta configuración pertenece a la capa de firmware y electrónica ya descrita previamente; en 6.3 basta con indicar que la ruta Python--Bridge--firmware desemboca en esa UART física y, desde ella, en el conector MIDI OUT del prototipo.

La Figura~\ref{fig:impl_midi_eventos_bytes} resume la formación de mensajes MIDI desde las notas generadas. Esta figura aclara la diferencia entre nota musical, evento MIDI temporizado, bytes MIDI y transporte físico.

\begin{figure}[H]
    \centering
    \IfFileExists{images/uml/impl_midi_eventos_bytes.png}{%
        \includegraphics[width=0.92\textwidth,height=0.62\textheight,keepaspectratio]{images/uml/impl_midi_eventos_bytes.png}
    }{%
        \IfFileExists{images/uml/impl_midi_eventos_bytes.pdf}{%
            \includegraphics[width=0.92\textwidth,height=0.62\textheight,keepaspectratio]{images/uml/impl_midi_eventos_bytes.pdf}
        }{%
            \fbox{\parbox{0.9\textwidth}{\centering Diagrama pendiente de exportar desde Mermaid: \texttt{images/uml/impl\_midi\_eventos\_bytes.mmd}}}%
        }%
    }
    \caption{Formación de mensajes MIDI en la ruta live. Las notas de alto nivel se transforman en eventos \texttt{note\_on}, \texttt{note\_off} y \texttt{program\_change}; después se convierten a bytes y se envían al firmware mediante \texttt{midi\_bytes}.}
    \label{fig:impl_midi_eventos_bytes}
\end{figure}

Como medida de seguridad, el sistema conserva la ruta \texttt{panic}. Esta acción limpia el scheduler y genera mensajes \texttt{All Sound Off} y \texttt{All Notes Off} para todos los canales. Es necesaria porque una ventana de mala calidad puede bloquear la generación de nuevos compases, pero no elimina automáticamente eventos que ya hubieran sido programados desde una ventana anterior. Por tanto, el \textit{panic} forma parte de la robustez del sistema MIDI y debe mantenerse aunque se simplifique la lógica musical.

La salida MIDI puede desactivarse mediante la variable de entorno \texttt{EEG\_MIDI\_LIVE\_ENABLED}. En ese caso, los eventos pueden seguir generándose y contabilizándose, pero el transporte los marca como descartados en lugar de enviarlos al firmware. Esta opción permite validar generación y scheduler sin forzar siempre el envío físico por la UART MIDI.

\subsection{Implementación de la interfaz web}

% Pendiente de redacción propia.

\subsection{Implementación de la matriz LED}

% Pendiente de redacción propia.

\end{document}
